# wickrunAI 下一阶段优先级与验收条件：协作闭环及长期任务架构

## 2026-09-20 第六批 2.17.5：从模糊想法开始，理解真实协作流程

工程基线 **7c58d7f / 2.17.4**，用户已确认发布、安装；本轮只读核对安装包 app.asar/package.json 也为 2.17.4。以下旧版本段落是历史记录。本轮源码 **2.17.5**，未提交、打包、安装或发布。

用户进一步明确：任务优先仍不够，不能要求初学者先有具体目标和 workflow；一句模糊想法应能开启协作，已有方案应展示实际步骤与分工。

- **U0a 想法起步已实现并经受控桌面通过**：新任务默认“帮我理清想法”，只需一句话；任务名可自动取开头，无需先填验收。也可选择“我知道要做什么”。方向探索、方案整理是两个实际 API 成员步骤，复用原运行器、预算、重试、持久记录与人工结束门。当前仅生成方向、问题及可编辑下一步建议，不自动执行建议。
- **建议到下一任务的接续已通过**：展示格式化结果，支持补充后继续梳理，携带上一轮建议与回答；可把建议目标、完成标准带入编辑器，用户修改保存后才成为独立任务，保留 sourceTaskId/sourceRunId。模型不能通过建议植入成员、工具、路由或流程。无法解析的建议保留原文，下一任务留空填写，失败或未完成步骤不被当作可采用结果。
- **桌面发现的衔接缺陷已修复**：旧轮等待验收占用单并发名额，曾导致下一轮 claim 失败。现在选择下一步会先结束本轮“梳理交付”，不代表同意执行实际方案；独立运行器/持久存储回归复现原失败，再验证名额释放及幂等结束。修复前未派发的运行保留并通过界面停止，未删除或伪造成成功。
- **U1b 真实流程预览已实现**：任务选择、快速方案、流程列表/设计器及运行前检查展示真实图。普通节点用日常表达；可查看成员、指令、产出、条件与下一步，返工保留次数上限。默认选择最新已保存版本，无版本才显示草稿；草稿另有修改时明确提醒。条件/并行/循环保留连接，不强行描绘为直线步骤。任务切换及选择流程后回到页面开头，避免预览被旧滚动位置遮住。
- **规划边界有工程约束**：explore intent 随运行冻结；前端运行器及主进程存储均拒绝带文件/命令工具、技能、本机客户端或执行型节点的探索运行。旧任务无 intent 继续按原语义运行。探索模式不改项目目录、允许接入、权限或现有团队。
- **验证**：684 项自动化、类型检查、生产构建通过；真实 Electron 界面完成想法保存、准备、两轮本机 HTTP 规划、补充、修改建议并保存下一任务、已有复核流程版本预览。4 次模拟请求，合计夹具报告 1200 tokens，无真实供应商费用/质量结论。最新源码哈希、请求、截图和限制见 [本轮验收](acceptance/2.17.5/README.md)。
- **未完成的范围**：真实免费模型的规划质量和首次使用者可理解性未通过本轮证明；尚无自动生成整个多阶段项目及依赖的能力。角色分别选模型/成员复用、文件任务首跑、运行页整体简化与错误恢复引导继续施工。下一阶段优先级已按本次反馈调整。

下一施工顺序：**U0b 真实免费模型与新手验证 → U0c 项目阶段总览及多任务依赖 → U2 角色定制与成员复用 → U3 文件任务首跑 → U4/U5 运行恢复说明与导航收敛**。详细验收条件见 COLLABORATION_USABILITY.md。

## 2026-09-20 第五批 2.17.4：429 退避修正与任务优先入口

用户确认易用性方向为“任务优先，提供可展开的高级设置”。已在保留现有定制能力的前提下完成首批文本入口；后续文件任务和按角色定制继续施工。当前安装版仍为 2.17.2，未提交源码 2.17.4 累积包含上一批 2.17.3；本轮未打包发布。

- **429 问题已定位并修复**：协作空间原本就复用单一 Agent 的 runAgent、全局自动重试设置和默认 62 秒退避；Retry-After 优先，等待受恢复时限、运行预算与取消约束。上轮 OpenRouter 曾自动倒计时，随后由主负责人手动暂停；SenseNova 的 TPM/RPM 原文夹带 insufficient_quota，被共享分类器优先误判为不可重试额度耗尽。现明确分钟限流优先于通用额度代码；402、明确余额耗尽、裸 insufficient_quota 和非分钟日额度仍不自动重试，401/403 维持鉴权优先。
- **真实桌面退避已通过**：本机合成服务返回同类 SenseNova 429 原文，应用显示“62 秒后继续”，实际两次 HTTP 请求相隔 62144 ms，同一路由 429 → 200；没有手动重发或换模型。交付经界面批准 completed，已知夹具用量 200 tokens，首个失败请求用量仍为缺失。这不能证明真实 SenseNova 当时等待后必然可用；旧报告的 quota 分类保留为历史证据。
- **U1 文本快速准备已落地**：任务页选择接入、模型与直接完成/讨论后完成/完成后复核。准备普通成员与可编辑流程，并保存固定版本，下一步确认才派发。空团队实测无需进入成员页或设计器：保存任务 → 准备并检查 → 确认开始 → 验收。准备后 1 成员/1 流程/1 版本，运行和请求数仍为 0。
- **定制与边界**：高级设置保留原有成员、文件流程、已有流程选择及设计器；已有负责人/已有流程不被快速准备覆盖。新快速成员仅处理文本、关闭自动接力、不开放文件/命令，遵守项目允许接入与预算。文件任务首跑、分别选角色模型、团队预设、运行页信息层次和真正的新手对照仍未完成。
- **验证**：655/655 自动化、类型检查、生产构建通过；本机 HTTP 集成验证默认 62000 ms 与 Retry-After:2 的 2000 ms；桌面另以真实时间等待 62.144 秒。i18n 覆盖新增组件。界面检测唯一提示为原有时间线边框，未新增该样式。证据见仓库 docs/acceptance/2.17.4/README.md。
- **支线施工顺序与验收**：U1 文本首跑已完成 → U2 按角色定制与复用 → U3 文件任务首跑 → U4 运行/等待/恢复说明 → U5 导航与新手任务对照。完整条件见 docs/COLLABORATION_USABILITY.md；主线真实模型质量、长任务重启与证据接入继续保留，不因本支线通过而标记完成。

## 2026-09-20 已安装 2.17.2，第四批 2.17.3 工程及受控桌面验收完成

用户确认 2.17.2 已打包发布并安装；本机安装包 app.asar 版本核实为 2.17.2，仓库基线为 ecc5578。日常项目、对话和历史协作记录在升级后仍可见，未据此宣称全部旧数据语义迁移已验收。以下旧版本段落记录的是当时状态，不能再理解为当前尚未安装。

- **A5 精确重复返工停止已完成**：相同复核节点连续失败、内容和要求不变且没有新增可区分证据时，保存失败并停止派发，等待核实。按文件哈希、文本摘要、读取/引用内容比较，忽略新编号和时间戳；新增用户补充要求也会阻止误判。内容/要求/证据变化或无法可靠规范化时仍沿用原有限回路，不判断语义等价。
- **桌面发现并修复重试缺陷**：真实 runAgent 在明确重试时曾拼接旧、新 JSON。现持久保存停滞标记，仅该类已完成复核使用新检查点重新生成；原回复、检查证据和用量保留，普通中断恢复逻辑不变。回归先复现失败，再验证修复。
- **实际桌面闭环已通过**：独立 Electron 窗口操作新建运行，两轮相同文本与失败引用后暂停；重载窗口后明确重试，仅新增一轮复核，再次停滞；人工接受后仍进入最终交付确认，经按钮批准完成。2 次执行、3 次复核、5 个本机 HTTP 请求，夹具实报 1000 tokens，旧请求未重复计数，无第三轮执行。失败判词仍保留 fail；这证明控制流程，不证明模型质量或 OS 进程重启。
- **工程检查已通过**：646/646 自动化、类型检查、生产构建、差异格式检查。只读证据校验确认 5 个不同请求 ID、冻结输入一致、旧检查点保留、预算预留清空；构建保留已有大包提示。详见仓库 docs/acceptance/2.17.3/README.md。
- **安装版免费路由试验有记录，质量对照未完成**：在已安装 2.17.2 界面新建合成 CSV 项目、导入三策略模板并保存单成员版本，分别新建三次单成员运行。SenseNova deepseek-v4-flash 为 HTTP 429 / insufficient_quota；omni auto/coding:free 经网关选择 oc/big-pickle 后返回 HTTP 403 客户端限制；OpenRouter qwen/qwen3.8-27b:free 三次 HTTP 429 后经界面暂停。共 5 个请求，均无产物，输入未改动；用量缺失，不能记作零。仅选择授权免费入口，未直接选择 dva/ 或 oc/，自动接力关闭。这些固定模型当次失败，不代表整个平台不可用。
- **交付边界**：当前源码 2.17.3 尚未提交、打包、安装或发布。日常应用及 OpenAI 窗口保持打开。真实三策略对照、长任务实际进程重启、订阅客户端证据桥、记忆/Skills 质量评估与系统沙箱仍未完成；下一步顺序和验收出口见路线图。

## 2026-09-20 测试路由更正与交付安排

用户已明确测试范围：SenseNova 全部模型、omni 的 auto/ 路由、OpenRouter 的免费路由。此前 dva/gpt-5-6-luna-max 和 oc/gpt-5.6-luna 选型不符合此范围；两次 500/402 只证明这些路由当时失败，不能据此认定免费路由或整个网关不可用。免费路由的真实任务验收仍待进行，后续不得沿用前述两条付费路由。历史失败记录保留，不将缺失用量解释为零费用。

当前 2.17.2 可冻结代码并制作待验收安装包；对外正式发布前补安装启动、旧数据保留、免费路由真实任务和重启接续的基本检查。更大规模的三策略收益比较可在后续版本继续，不能在本次说明中宣称已证明质量或节省成本。本记录没有执行打包、安装、上传或发布。

## 2.17.2 第三批施工历史状态

基线仍为 cf70b1b / 2.16.2；当前未提交工作树为 **2.17.2**，包含前两批。未打包发布或替换日常应用。历史段落的“待做”按其版本时点理解，当前以本节和 PROGRESS.md 为准。

- **A5 文本复核已实现并完成受控桌面闭环**：独立 API 成员复核本次文本快照；attempt/访问版本/SHA-256/原文引用核对，失败可有限返工；全部输入需覆盖，超过 20,000 字符拒绝而不截断。文件改动不能走文本复核，外部事实仍需相应证据；事实正确性不是引用检查可以证明的。
- **A2/A10 记忆治理已实现**：事实、偏好、经验；项目通用或任务关键词；有效期；候选/验证/采用/停用沿用原流程。保存全量已采用快照，按创建运行时点派生选入内容，最多 6000 字符，超限整条略过，显示选入/排除原因。旧记录按项目经验兼容，续跑不随当前项目修订变化。字面匹配不等于语义相关性或学习收益；最新要求冲突评估、Skills 对照仍待做。
- **A9 多路由聚合已实现**：完整稳定请求去重后按派发身份汇总，不依赖事件显示上限；明确未分配和历史缺口，混合新旧请求不报告完整成本，导出保留脱敏汇总。只计算已知 tokens，不推算价格或把整次成功归功于某模型。
- **A6 实际桌面证据**：产品入口生成/保存/运行，文本 fail→返工→pass→界面批准；4 个受控请求、720 tokens、两路由各 360；只选入 2 条、173 字符记忆。主负责人实际操作，证据见 docs/acceptance/2.17.2/。
- **工程检查**：638/638、类型与生产构建通过；生成流程默认预算超过项目上限的问题已修复；既有大包提示保留。

**后续优先级**：先验证用户指定的免费路由，再进行固定任务集三策略对照和真实长任务重启试验；可独立推进订阅客户端可核实证据与停滞识别，再做记忆/Skills 质量评估、价格快照及独立系统沙箱工程。500/402 保留为错误选型的历史记录，不将其视为免费路由阻塞。

## 2.17.1 第二批施工历史状态

基线仍为 cf70b1b / 2.16.2；当前未提交工作树版本为 **2.17.1**，包含上一批全部修改。未打包发布、未替换日常应用。下方各版本条目保留历史时点；当前状态以此节及 PROGRESS.md 为准。

- **A3/A8 组合回归已增加**：实际 runAgent 中串联用户否定/纠正、至少两次语义压缩、持久存储重载、连接与模型切换、原始附件证据检索、成功写入不重复及未知操作暂停。传输和文件工具受控，归档位置人工构造；不是 OS 进程重启或真实模型语义保真证明。
- **A5 复核路径漏洞已修复**：要求完整接收路径匹配；拒绝同名异路径、POSIX 大小写不同、无产物版本的通过。仍须实际调用编号及当前快照，模型判词不等于用户接受。
- **A9 逐请求归属已实现**：连接/模型随派发持久记录，以脱敏路由进入请求事件；累计检查点按稳定 ID 去重。旧数据缺口、任务级多路由排行榜与货币价格仍不推算。
- **A4/A9 真实模型试验有明确阻塞**：固定合成 CSV 题、独立答案校验、三种流程已准备；桌面实际请求 dva/gpt-5-6-luna-max 返回网关 500（DEVIN_AGENTIC_HOME 沙箱配置），固定替代 oc/gpt-5.6-luna 的连通性请求返回 402 payment_required。没有可用产物，不能比较策略收益；没有自动购买服务或改动沙箱。
- **当前桌面复核通过**：2.17.1 页面新建运行，非空差异的 v1/v2 完成 fail→返工→pass→界面批准；两个隔离区产物均为 ready，8 次请求具有连接与模型身份。
- **工程验证**：全量 617/617、类型与生产构建通过。证据目录 docs/acceptance/2.17.1/；构建保留已有大包提示。

后续顺序：先恢复可用的固定真实路由，跑同题三策略并扩充任务集；再做真实长任务与重启试验；随后扩展纯文本/订阅客户端证据、记忆检索与系统沙箱。真实模型质量与收益尚未验收，不能因自动化通过将其标成完成。

## 2.17.0 首批施工历史状态

本次已从只读评审进入实现。原始基线仍为 `cf70b1b / 2.16.2`；当前工作树是尚未提交的 2.17.0 更新。下面保留的 2.16.2 审查结论与原始验收条件用于追溯，涉及“未实现/未测试”的旧状态，以本节及仓库 `PROGRESS.md` 的验证账本为准。

| 方向 | 本批实际落地 | 尚未覆盖 |
|---|---|---|
| P0 预算与本机完成 | 预留额度受成员/总量硬上限约束；修改证据与测试/推送要求分开 | 本机测试/推送仍需外部执行证据，不能只凭文字通过 |
| A1/A2 任务与上下文 | 从现有持久记录派生任务契约；保留有效要求、修订、来源、未知动作；节点只接收依赖输入 | 全后端统一任务状态、相关性检索仍为后续工作 |
| A3 压缩与接力 | 边界/来源/工具批次校验；压缩与跨模型交接回归；注入投影有长度上限，完整原文可找回 | 未证明模型摘要的语义蕴含；真实长任务组合试验待做 |
| A4 分派与产物 | 不可变快照、同任务跨成员接收、新增/修改/删除、版本替换和恢复日志；新增可选“执行与复核”模板 | 讨论与复核哪个更好仍需固定任务集对照，不默认宣称省钱 |
| A5 质检 | 独立 API 成员只读复核；调用编号真实、版本匹配、改动文件读取覆盖、接收副本完整性；有限返工 | 纯文本评审、原生订阅客户端工具证据桥、语义停滞检测待扩展 |
| A6 管理界面 | 产物版本/复核结论可见、生成流程绑定任务、未合并产物优先、复核副本不重复合并 | 更完整的状态来源时间线仍可继续完善 |
| A7 权限 | 主进程校验当前成员/节点/权限/隔离范围；本机复核在派发前明确拒绝不支持的能力 | 命令的工作目录不是操作系统沙箱 |
| A8 恢复与定时 | 接收中断恢复、HTTP 429 接力、已成功操作不重复、未知副作用停止；定时去重及暂停/未知任务防重叠 | 活供应商断流、认证和客户端重启组合仍需专项试验 |
| A9 用量 | 普通/最终/压缩请求稳定 ID，累积检查点去重后进入团队统计；缺失保留未知 | 多路由的逐请求精确归属、价格快照和真实策略收益待做 |
| A10 记忆 | 已验证项目隔离、停用后不注入新运行；在途任务保留其冻结版本 | 事实/偏好分类、相关性评估、Skills 自动提案/受控采用待做 |

已实际操纵独立 Electron 测试实例：界面创建项目 → 开始运行 → v1 缺陷被复核退回 → v2 修正后复核通过 → 查看差异 → 人工批准 → 合并到测试主目录。最终文件为有效 JSON，`status` 为 `ready`。这是受控本地模型驱动的真实桌面链路，**不是活供应商质量验收**。截图、执行编号、产物摘要与哈希保存在 `docs/acceptance/2.17.0/`。

下一阶段按顺序推进：真实供应商固定任务对照 → 长任务纠正/压缩/切换/重启组合 → 纯文本与订阅客户端证据覆盖 → 记忆检索/多路由成本 → 系统执行环境隔离。不得把这些后续项标成已完成。路线图见仓库 `docs/ENGINEERING_ROADMAP.md`。

## 原始 2.16.2 审查记录与验收条件

日期：2026-09-19。工程基线：`sensenova-chat`，版本 `2.16.2`，提交 `cf70b1b`。检查前后工作树均无未提交改动。本次只读审查应用代码，未调用真实供应商、未修改应用或用户任务数据。

文档修订：按用户要求，将最初提出的上下文、压缩、任务状态、分派、质检、管理与经验改进方向纳入同一计划。本次修订前再次确认 HEAD 为 `cf70b1b`、工作树干净；只修改本文，下面的 112 项测试与类型检查是上一轮复核结果，本轮没有重跑。新增架构条目是设计建议，不能解读为已经实现或已经通过实验。

## 两份分析的关系与真实差异

Claude 今天的报告以协作空间实测为主，回答“目前哪里卡住、修了什么、哪些场景还没验”。最初的架构建议回答“长任务、多模型和多人协作怎样持续可靠”。两者部分重叠，也有独立维度；不能把后一组全部折叠成前一组的 bug 清单。

上一版本文过度聚焦短期问题，把架构方向笼统后排，遗漏了可以现在定义接口和开始评估的工作。本版改为两条工作线：先修具体故障，同时建立最小任务、交接、证据契约及回归基线；大规模重构仍以这些验证结果为前提。

| 维度 | Claude 今日报告的覆盖 | 初始建议额外关注 | 关系及本版处理 |
|---|---|---|---|
| 工作连续性 | 轮次耗尽自动续跑、来源 ID、R7 故障接力 | 持久任务独立于模型、聊天窗口、执行后端；统一状态与交接契约 | 目标相关，架构范围更大；A1 单列 |
| 上下文选择 | 重复节点、历史重发导致开销 | 当前有效要求、近期状态、历史证据、长期经验的分层及按需检索 | 有重叠但不等价；A2 单列 |
| 压缩可靠性 | 提到折叠已完成产出 | 压缩语义保真、矛盾与未知状态保留、跨供应商可移植记录 | 原报告未单独验证；A3 单列 |
| 工作分派 | 单人去掉讨论；两人仍生成讨论，建议双成员试点 | 按任务依赖决定是否讨论或并行，明确委派输入、产物、权限、预算和停止条件 | 有实际策略差异；A4 保留可对照方案 |
| 质检 | 程序检查跑通、编号提示、人工验收 | 独立 review 的证据真实性、产物版本失效、独立上下文复核、停滞检测 | 同一领域的不同层次；A5 与短期修复衔接 |
| 管理界面 | 顶部运行概览、等待文案、任务选择 | 可追溯状态来源，区分生成、检查、验收、合并及用户接受 | A6 扩展，不重做已完成界面 |
| 权限与隔离 | 工具批准方式、成员隔离文件及合并 | 任务授权随接力保留、能力差异明确表达、执行环境与任务状态分离 | A7 单列；文件副本不代表系统沙箱 |
| 恢复与定时执行 | 阶段续跑、接力验证 | 副作用账本、结果未知的核实、调度去重及同一任务生命周期 | A8 复用已有机制，补组合验证 |
| 预算与观测 | 阶段下限、总用量和试点降幅 | 摘要/讨论/质检/重试的总成本归因；同任务质量与成本对照 | 有重叠；P0 修正确性，A9 验证策略 |
| 长期记忆与经验改进 | 今日协作试点未单独展开 | 事实/偏好/经验分类、范围与失效、候选经验评估、采用与回退 | 独立维度；A10 单列 |

这里列的是文档覆盖和可观察实现的差异，不推断 Claude 未表达的立场。未提及一个架构方向，不代表它反对这个方向。

需要明确保留的取舍：

- **多人是否必经讨论**：当前模板人数大于一就加入讨论；本计划建议由任务依赖和预期收益决定。用双成员任务比较“讨论后执行”和“执行后独立复核”，不预先宣称一种总是更好。
- **预算下限的角色**：今天的实现用下限保障启动；本次复现说明它会突破成员上限。本计划将成员/任务上限作为硬约束，将阶段最低需求作为可解释的估算或派发前检查。
- **成功如何判定**：单人成果通过人工验收证明这条路径可用，尚不能替代跨成员产物交接、独立 review 或故障恢复的证明。
- **何时推进架构**：不等待所有体验问题清零才定义契约和压缩回归；也不在证据不足时整体替换已有执行引擎。最小接口和评估先做，迁移逐步做。

## 依据与验证范围

- 阅读今天的 `docs/releases/v2.15.2-pilot-report.md`、`v2.16.0-status.md`、`v2.16.x-acceptance-report.md` 及 2.16.0–2.16.2 变更资料。
- 对照 2.15.0 到当前提交的代码差异，检查协作运行、隔离文件、工具权限、验收、流程模板与界面。
- 本次运行协作、交付、harness、上下文交接相关测试：112 项通过，0 失败、0 跳过。类型检查通过。
- 另用当前 `stageFloor` 和真实 `createTeamExecutionGuard` 的内存夹具复现预算不一致。没有执行文件操作或模型调用。
- 原报告的 579 项全量测试及真实 MATH 100 任务结果，是历史记录，本次没有重跑全量或桌面实测。测试通过不等于所有边界均已覆盖。

## 已实现与仍待验证

| 项目 | 当前状态 | 边界 |
|---|---|---|
| 单人成员流程去掉讨论节点（R1） | 已实现；历史报告有实测 | 多人成员仍自动生成讨论节点；尚不能推断讨论必然有益 |
| 稳定且缩短的任务来源 ID | 已实现；相关回归通过 | 跨节点产物与证据交接仍需单独处理 |
| 可引用证据编号提示（R2 / R12） | 已实现；相关回归通过 | 编号提示修复不等于独立 review 节点已核验真实证据 |
| 阶段预算下限（R3） | 已实现，但发现与成员上限冲突 | 应先修复，不应直接标记为收口 |
| 正常轮次耗尽后的自动续跑（R4） | 已实现；历史报告一次实测成功 | 有未知操作、用户暂停和次数上限时不会自动续跑；更多故障组合待验证 |
| 总览条、任务默认选择、设置保存、等待文案 | 已实现 | 原报告中的这些条目无需重新开发 |
| 成员级 API failover（R7） | 已实现，夹具测试覆盖 | 缺实际传输链路故障试验；本机订阅客户端明确不参与自动接力 |
| 成员隔离、差异核对、合并与恢复 | 已实现，相关回归通过 | 每个成员独立从主目录复制，不等于已支持未合并产物跨成员流转 |
| 双成员讨论、并行与汇合 | 有实现与夹具覆盖 | 缺双成员真实交付闭环证据 |
| 文件卡片排序（R14） | 当前直接按保存顺序渲染 | 新结果置顶仍待实现 |
| Esc 关闭（R15） | 当前 Modal 已注册 Escape 处理 | 报告中的失效应重现并排查焦点、遮挡、运行包版本，不能定性为没实现 |

## P0：修复预算约束不一致

位置：`src/lib/team-runtime.ts:432` 附近，及 `electron/team-execution-guard.cjs` 的工具预算检查。

复现配置：总预算 400000、成员每步上限 4000、并发槽位 1。阶段下限计算为 20000，当前预留表达式把实际预留抬到 20000；执行权限层又要求预留量不超过成员上限，于是拒绝工具派发，错误为“工具派发缺少有效的本次用量预留”。

建议：成员上限继续作为硬约束；若估算的最低可运行量大于上限，派发前准确说明冲突，不自动放大成员预算。若该阶段下限只是启发式值，应明确它的性质并调整算法。

验收：覆盖成员上限低于阶段下限、多分支竞争预算、剩余预算不足；预留不突破成员和任务总上限；可运行配置能够真正执行工具。

## P1：先打通两位成员的产物与证据闭环

### 1. 明确交接产物

当前 `runMember` 按 runId/memberId 创建隔离副本，副本来自主目录；`sources` 传递前置节点的文字输出。`inputRefs` 选择的是节点输出，没有看到这条路径将 A 未合并的文件版本交给 B。

这意味着不能假设 B 在自己的工作目录里能看到 A 刚生成的文件。这个结论来自代码路径检查，尚未运行双成员桌面复现。

建议最小实现：允许后续节点通过明确授权读取上游产物快照，记录产物 ID、生成节点、相对路径、版本/哈希；复核默认只读。无需开放所有成员互写目录。

验收任务：A 新增一个主目录不存在的 JSON；B 读取 A 的该版本，发现预置错误；A 修复；B 复核新版本；最终人工验收并合并。B 不能因读到旧副本而给出通过结论。

### 2. 收紧独立 review 节点

位置：`src/lib/team-runtime.ts:324`。目前分支判定接受 `verdict=pass` 和 truthy `evidence`，未在此入口验证证据引用与产物版本。

必须区分两个机制：执行 Agent 内部的 `verify_requirements` 已有程序检查与模型复核区分；流程里的独立 `review` 节点仍主要解析模型文本。今天的单成员试点不能证明后一条路径通过。

建议：复用现有验收基础，关联实际 requirement、工具检查记录及产物版本；保留 passed / failed / unverifiable；模型意见与程序验证分开显示。文件变化后旧结论不得直接复用。

验收：伪造证据、无关证据、过期版本均不能走通过分支；真实错误能驱动返工；修复后重新核验。

## P1：补齐故障接力与恢复证据

R7 应归类为“已有功能的验证缺口”，而不是待实现。

先通过受控本地 HTTP 服务制造 429，走真实请求、错误分类、检查点、换路由和验收链路，再安排真实供应商试点。仅把错误对象直接传给回调的单元测试不能替代传输链路测试。

至少覆盖：首次请求限流、部分工具已经成功后限流、未知副作用结果、恢复时重启、没有可用备用路由。检查不重复副作用、不重复计费、routeLog 可追溯、原成员快照不被改写。

## P1（使用本机订阅成员时）：修正完成判定分支

位置：`src/lib/team-runtime.ts:270–286`。`clientEvidenceIssue` 在检查文件变化后无条件返回“要求测试或推送”的错误。因此仅要求修改文件、且确实产生变化的任务，也会落到不匹配的核实原因。

这是静态代码确认的分支问题，本次未运行本机客户端实测。修复时应区分修改、测试和推送三类要求；文件变化只能证明变化发生，内容验收仍需对应检查，不能因此直接宣布任务完成。

## P2：有针对性地减少上下文和协调成本

当前 `sources` 在未设置 inputRefs 时拼接所有已完成尝试的输出，多人讨论继续拼接本轮发言。单人成员去掉讨论只消除了一个重复来源，尚不能说明多人成本问题已解决。

优先按依赖选择输入，传递结果摘要、产物引用与未完成事项，再按需检索历史。双成员任务先验证执行者与复核者的价值，不因人数增加就强制增加讨论。

评估沿用现有记录体系：比较同一任务、同一模型和配置下的重复执行、额外轮次、检查失败、成本与交付质量。历史 343096 → 133688 tokens 是不同科目任务的观察值；下降约 61% 不能作为优化因果效果或长期保证。

## P2 / 后续

- R14：未合并产物优先、新运行优先，可搭后续改动完成。
- R15：先桌面复现，再决定是否需要代码修复。
- R9：报告描述的是 Claude 操作 wickrunAI 时被 Claude 窗口遮挡；先归入自动化实测环境问题。若 wickrunAI 自身电脑操作出现相同故障，再定位自身动作层，不据此直接扩大协作空间开发范围。
- 完整内核迁移、供应商原生压缩适配、大规模自动路由、自动经验采用与复杂组织结构按下方 A1–A10 的依赖推进；不再将这些方向整体笼统后排。最小契约、压缩质量基线和明确分派协议属于当前阶段。

## 架构工作线：初始建议的完整纳入

以下“基础”仅指已查阅模块和既有资料支持的事实；“增量”是需要实现或进一步审计的范围。有基础不等于所有入口已统一，也不等于下列验收已经通过。

### A1 · 独立且持久的任务状态

- **基础**：已有 RunState、TeamRun、检查点、要求、里程碑与 handoff；`src/lib/handoff.ts` 已携带工作状态和证据。不是从零建立任务系统。
- **增量**：定义跨入口的最小任务契约，明确 taskId、attemptId、要求版本、授权、产物版本、验证记录、待确认操作与阻塞原因分别由谁维护。模型和后端可替换，事实记录不能依赖某段自然语言答复。通过适配器接入现有单 Agent 和 TeamRun，不立即合并所有存储结构。
- **优先级/依赖**：P1 定义契约并用于产物/证据交接；P2 渐进统一 API 与本机客户端的能力映射。无需先建设服务器或分布式系统。
- **验收**：同一任务换模型、暂停和重启后，要求修订、完成证据、未完成项与未知操作不丢失；用户只改一项要求时，可保留无关的有效成果；不因接续而新增一份逻辑任务或重复计费。旧记录无法补齐的字段保持未知。

### A2 · 分层上下文与当前有效要求

- **基础**：`context-memory.ts`、`task-context.ts` 已保留原文、提供历史检索、折叠工具材料；要求修订也已有实现。不能把“增加上下文管理”作为全新功能。
- **增量**：明确四层：当前有效约束、当前工作状态、可检索历史证据、按范围检索的长期经验。检查所有入口是否使用有效要求版本；用户原文继续保留，但被替代要求不应以当前指令身份反复注入。前置输入按依赖选择，工具和技能按任务需要加载，提供可诊断的上下文组成记录。
- **优先级/依赖**：P1 定义有效要求与来源规则；P2 调整上下文选择，依赖 A1/A3 的记录和评估。可复用已有检索，不预设必须引入向量数据库。
- **验收**：用户把英文改为中文、缩小文件范围或撤销某项要求后，续跑、子代理和复核者都遵守最新版本；仍能查到被替代原文。无关历史不随每轮无限增长；被省略的材料可定位取回，未读取部分不能标记已检查。

### A3 · 压缩质量与跨供应商可移植性

- **基础**：已有带来源的摘要、结构与大小校验、原文存档和压缩后回读文件。现有校验不能独立证明摘要忠实表达原文。
- **增量**：建立固定压缩/接力回归集，分别检查约束、矛盾、未知状态和证据引用。明确可读任务记录是跨供应商交接基础；供应商原生压缩状态只能在已确认兼容范围内复用，不能假定可跨模型后端通用。
- **优先级/依赖**：P1 立即建立回归基线，与故障接力验证共同推进；P2 根据失败案例改压缩策略；原生压缩接入按具体后端能力后续评估。回归集不依赖完整内核重构。
- **验收**：至少覆盖用户纠正、否定约束、尝试未成功、已完成操作、未知外部结果、相互矛盾的证据、多次连续压缩和换模型。压缩前后检查关键状态是否一致、来源是否支持结论；保留失败样本，错误摘要不得成为不可回退的唯一记录。若做运行对照，保持任务、配置和输入一致，并记录实际用量与质量，不能只比较压缩率。

### A4 · 明确委派协议与适度调度

- **基础**：已有临时子代理、稳定请求键、并发限制与协作节点；不是新增一个“多 Agent”开关。
- **增量**：将目标、必要输入、产物版本、可读写范围、预算、停止条件和验收责任结构化；返回摘要、产物引用、证据与未解决事项。主 Agent 保留整合责任。工作形态至少区分单 Agent、少量独立工作者、执行者加复核者；讨论和并行按任务需要选择。
- **优先级/依赖**：P1 在双成员闭环中采用最小协议；P2 做调度策略对照。依赖产物交接与 A1，成本判断依赖 A9。
- **验收**：委派重试不会新建重复子任务；依赖未完成不提前派发；不会把同一操作交给两个成员重复执行；取消和补充要求能到达相关执行者。越权读写被阻止；子代理未交付时主任务不得凭其自述完成。并行修改需有隔离与冲突检查。

### A5 · 多层质检与停滞检测

- **基础**：已有程序检查、模型复核、用户验收区分、完成门槛及连续失败检测；独立 review 节点仍有前述缺口。
- **增量**：所有入口使用一致的证据语义，检查绑定要求和产物版本。程序可判的条件由程序判断；语义问题可使用独立上下文复核，提供目标、产物与必要来源，降低对执行者自评的依赖。记录缺陷及修复关系，区分重复检查和新证据。
- **优先级/依赖**：P1 修独立 review 并增加版本失效规则；P2 校准模型复核与停滞判断。依赖产物版本，复核与修复共用总预算。
- **验收**：伪造/无关证据不能通过；相关文件变化使相应旧验证失效；不得通过降低验收标准解除失败。同一缺陷反复出现且没有新证据时，系统换方法、返回具体阻塞或请求必要输入，不能无限互相转派。复核者误报、漏报应通过标注样本检查，模型复核不能自动升级为程序证明。

### A6 · 可追溯的工作管理界面

- **基础**：运行总览、等待类型、产物卡片、要求检查与观测记录已经存在。
- **增量**：围绕用户关心的问题组织状态：正在做什么、生成了什么、检查了什么、为什么停、需要什么输入。把执行结束、验收通过、用户接受、合并完成分别表达，并允许展开证据来源；普通用户不必阅读完整日志才能决定下一步。
- **优先级/依赖**：P1 随双成员闭环呈现当前产物与检查状态；P2 统一各入口及诊断视图。依赖 A1/A5，R14 可随同完成。
- **验收**：在运行、等待工具批准、未知结果、待验收、拒绝、合并冲突和重启后场景中，顶部状态与持久记录一致；不会把模型说完成显示为已经验收。用户能定位实际文件版本和未检查项；不伪造隐藏思考作为进度依据。

### A7 · 权限、执行环境与能力边界

- **基础**：已有成员工具授权、运行权限快照、文件副本隔离、目录约束和合并冲突检查；API 和本机客户端的权限能力并不完全相同。
- **增量**：将授权附着于具体任务、动作类别、路径与有效范围；跨成员读取产物使用明确授权。把会话记录、调用模型的循环、实际执行环境分别界定。为不同后端声明可观察/可控制能力，不能把缺少执行证据或无法强制的权限包装为已支持。进程或系统沙箱作为单独决策。
- **优先级/依赖**：P1 保证新交接接口和后端能力边界；P2 审计凭据可达性、插件/工具授权与失效；P3 在需求和成本明确后扩展执行沙箱。
- **验收**：换模型、续跑或定时唤醒不能扩大授权；任务撤销后过期审批不得生效；产物交接不开放成员间任意目录访问。复用任务授权减少不必要重复询问，同时对能力无法映射的情况准确提示。文件副本、写入锁与系统沙箱在产品和文档中明确区分。

### A8 · 副作用恢复及定时任务统一生命周期

- **基础**：已有未知结果处理、恢复检查点、部分操作去重、定时触发记录和恢复测试；今天 R4/R7 只覆盖其中部分路径。
- **增量**：梳理每种有副作用工具的“准备、已发出、已确认、结果未知”及核实方式；重试策略绑定动作语义。定时任务复用同一任务和授权体系，明确离线、补跑、重叠执行与重复触发政策。不是把所有同参数调用一律缓存去重，测试等读取当前状态的动作仍需实际执行。
- **优先级/依赖**：P1 完成 R7 与未知结果组合测试；P2 补定时和跨后端恢复。依赖 A1/A7，无需为了本地调度立即建设云服务。
- **验收**：进程在外部动作前后中断时，不虚构成功，也不盲目重发；能核实则只读核实，不能核实则保留未知并说明具体阻塞。相同触发键不会重复建任务，重启不重复累计；用户暂停不会被自动恢复规则越过；离线行为与界面说明一致。

### A9 · 全任务成本与可比较评估

- **基础**：已有总预算、预留与用量账本、观测、评估集和基线比较；原生客户端缺少统一精确用量时有保守处理。
- **增量**：分开归因执行、讨论、摘要、质检、重试的成本，实际、估算和未知不混用。以已验收结果衡量成本，保持执行结束与用户成功分离。为上下文和调度改动进行同任务对照；模型调用本身具有波动时需保留重复试验分布，不能只挑最好一次。
- **优先级/依赖**：P0 修预算冲突；P1 保留各类调用账目与基线案例；P2 用数据调整分配和调度策略。具体样本数和采用阈值在试验前确定，不能事后按结果改口径。
- **验收**：主/子 Agent、摘要、review 和重试均计入同一任务预算；恢复与晚到事件不重复累计。报告同时展示交付质量、错误完成、用量、耗时与未核验比例，保留未知；没有价格信息不推算货币成本。优化不能只因 token 减少而宣布成功。

### A10 · 长期记忆、Skills 与可回退的经验改进

- **基础**：已有项目经验、Skills、任务观察、`routing-memory.ts` 和 `evals.ts` 的开发/留出集、基线比较。今天的协作实测并未证明自动经验学习有效。
- **增量**：区分事实、偏好、经验规则与待验证假设，补齐必要的来源、项目范围、适用条件、时间/版本及失效关系；检索时只注入相关有效项。失败经历不能自动变成全局禁令，候选经验或 Skill 修改应经历验证、采用与回退。需要补哪些字段先审计现有结构，避免重复建设。
- **优先级/依赖**：P2 做记忆结构与检索质量审计，补可追溯和失效规则；P3 才考虑自动提案、受控采用。依赖 A3/A9 的质量基线，不能用模型自评作为唯一收益证据。
- **验收**：项目经验不越界串入另一项目，旧偏好不会覆盖最新明确要求；记忆可查看、纠正和撤销。候选规则在固定任务集上与基线比较，保留失败和未知，检查原有能力退步；撤销后后续任务不继续使用被撤销规则。评估集使用与轮换有记录，不能反复调同一批题后仍称其独立验证。

## 合并后的优先级与阶段出口

P0 是当前已复现的运行正确性问题；P1 是下一阶段交付所需的最小能力和证据；P2 是基于可用闭环的可靠性与效率建设；P3 是需更充分证据的扩展。优先级与时间估算不同，本计划不预设工期。

| 阶段 | 实际工作 | 必须满足的出口 |
|---|---|---|
| 第一阶段：修故障、建立共同约定 | 修预算冲突；按使用范围修本机完成判定；定义 A1 最小任务契约、A4 委派格式及 A5 证据/产物版本；建立 A3 压缩回归基线；执行 R7 受控故障测试 | 合法预算能派发、上限不被突破；故障不重复已生效操作；接口与现有结构对应清楚；回归集给出当前实际通过/失败情况 |
| 第二阶段：验证两人协作交付 | 产物快照交接、独立 review 验证；跑生成—找错—修复—复核—验收—合并，加入重启和用户纠正 | B 确实读取 A 的当前产物；预置缺陷被发现；新版本重新核验；所有未完成/未知如实保留；只有授权变更被合并 |
| 第三阶段：验证长任务连续性与效率 | A2 上下文选择、A3 压缩策略、A4 调度对照、A6 状态统一、A8 定时恢复、A9 成本评估；开始 A10 记忆审计 | 长任务经历压缩、换模型、重启和补充要求仍能接续；同任务对照能证明质量与成本取舍；旧要求/旧证据/旧经验不污染新执行 |
| 第四阶段：按证据扩展 | 进一步统一后端状态、评估原生压缩、执行沙箱、受控经验采用及复杂调度 | 每项扩展都有明确需求、对照结果、能力边界与回退办法；不能仅凭一次成功试点直接全量启用 |

前两阶段是下一次协作空间交付的范围；第三阶段是紧接着的长期任务可靠性建设，其中基线、契约及记忆结构审计可以提前准备。阶段出口优先于增加节点数量，不能为等待大型重构而延后已有故障修复。

## 设计依据与适用边界

以下为前次讨论已经查阅的公开资料，作为设计来源保留，不是对两家桌面端未公开实现的断言；本次文档修订未重新检索产品更新。

- [Anthropic：会话、执行循环与执行环境分离](https://www.anthropic.com/engineering/managed-agents)：A1/A7 的参考，公开讨论的是 Managed Agents。
- [Anthropic：上下文工程](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)：A2/A3 的参考。
- [OpenAI：Compaction](https://developers.openai.com/api/docs/guides/compaction)：原生压缩能力的参考；API 文档不能直接证明 ChatGPT Desktop 所有内部会话的行为。
- [OpenAI：Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents) 与 [Anthropic：多 Agent Research 系统](https://www.anthropic.com/engineering/multi-agent-research-system)：A4 的参考，不保证多 Agent 对任意任务都有收益。
- [Anthropic：Agent 评估](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)：A5/A9 的参考。
- [OpenAI：项目与任务组织](https://learn.chatgpt.com/docs/projects) 与 [Claude Cowork 架构](https://support.claude.com/en/articles/14479288-claude-cowork-architecture-overview)：A6/A7 的参考，具体能力需按平台和执行模式区别。

下一阶段的协作目标：两位成员针对同一份明确版本的产物分工，故障后保留进度，检查结论可追溯，不突破预算、不重复已生效操作。更广的架构目标同时保留：任务跨长上下文、模型、执行后端与重启持续成立，并能用可比较的证据改善记忆、调度与成本。

## 本轮新增验收门槛 U0/U1b（2026-09-20）

- 不要求具体目标才能开始探索；“规划交付”与“实际执行交付”分开保存、确认和记录。
- 流程解释必须来自将运行的固定版本，包含真正的成员、分支、返工和退出；未保存草稿不能冒充已生效流程。
- 下一轮规划必须携带已给出的回答；旧轮结束后才继续，不能因用户尚未手工验收而卡住默认单并发项目。
- 模型建议只作为可编辑文本；用户保存新的目标与完成条件后才可准备执行，工具和路由不得从建议自动导入。
- 工程与受控桌面通过不等于真实易用性通过；U0b/U0c 及余下施工顺序以 [易用性支线](COLLABORATION_USABILITY.md) 的最新排序为准。
