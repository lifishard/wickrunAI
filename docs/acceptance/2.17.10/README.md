# 2.17.10 四项施工与验收

## 2026-09-20 第十一批 2.17.10：等待恢复、长任务组合与策略质量验收

基于最新未提交源码继续，保留此前改动；Git 基线仍为 74cab88 / 2.17.5。当前源码 **2.17.10**，未提交、打包、安装或发布。本轮按用户限定的四项推进，代码与独立应用验收无需重新打包。

| 本轮事项 | 已实现与已验证 | 结论边界 |
|---|---|---|
| U4 等待、恢复；U5 导航 | 运行页先显示状态、原因、下一步与已有产出；按真实等待状态显示重试倒计时。问题卡支持草稿落盘、刷新恢复、有效回答后从原问题继续。流程图/证据/事件按需展开；默认导航为总览、任务、文件，其他设置可展开。独立 Electron 跑通真实 HTTP 429→等待→问题→刷新→回答→交付批准。 | 模型响应由本机夹具生成；没有声称新手完成率改善已经测定。 |
| 长任务组合 | 同一运行含中途纠正、7 次生产压缩流程、两条本机路由切换、原始证据 read_context 取回、真实子进程强制中断与重载。已完成副作用后再次请求仅执行一次；未知结果先拒绝恢复，再经明确核实才能重试。 | 压缩摘要和模型决策为确定性夹具；真实生产运行器、账本、文件存储与 HTTP 管道。不是机器断电、真实供应商摘要语义或流式 UI 验收。 |
| 单助手与多助手 | 固定免费 SenseNova deepseek-v4-flash、同题、相同 60k 总/20k 阶段预算：单助手 3,832 tokens/5.759s；讨论后执行 11,393/20.103s；执行后复核 10,860/42.651s。三者程序答案全对，误报完成均 0，文本复核 pass。 | 每策略仅一次确定性题；不推导普遍质量排名或金额节省。证据使用整合前 2.17.9 引擎快照，保留源码哈希；不为版本号重发模型请求。 |
| 记忆、Skills 与循环 | 记忆提示保留版本/证据来源并明确当前要求优先；停用 Skill 不注入、不折叠提供、不允许 read_skill 取回。固定启用/停用、撤销/过期、范围、冲突提示测试通过。轻度改写从相似度硬停调整为保守提示，只有原完成检查已要求继续时才附加，不独立停机或增加轮次。15 固定样本：TP 3/TN 11/FP 0/FN 1。 | 检索与注入检查不等于模型普遍正确采用。保留 tp-cn-review-light-paraphrase 漏报，不宣称全面语义识别。真实免费模型四例单轮对照均通过：缺信息为 unknown、有效经验提供正确事实、当前纠正覆盖旧经验、撤销/过期与停用内容排除；不代表普遍质量。 |

回答入口同时补齐两项保护：中断后还处于 running 的非问题工具也视为待核实，即使尚未落下 uncertainCallId；并行复用同一助手时，问题句柄按运行/尝试/成员三者隔离。回答不能充当对未知操作的批准。

验证：**789/789 全量回归**、正式类型、132 文件快速检查、生产构建通过；最终整合构建再次完成独立 Electron 界面验收。证据见 [2.17.10 验收](README.md)。既有主包体积提示保留。

本轮四项按上述有界范围完成。仍保留的扩大验收：真实首次使用者、复杂任务与重复采样、多次压缩的真实模型语义、操作系统级隔离及安装升级。它们不被这一小样本自动标记通过。若验证日常安装版的新行为与旧数据升级，下一步才需要打包安装 **2.17.10**；不要把已安装旧版当作本轮源码。

以下旧批次为历史记录，当前状态以上文为准。


## 可复核证据

- [全量回归](automated-tests.txt)、[正式类型检查](typecheck.txt)、[快速检查](check-lite.txt)、[构建](build.txt)。
- [真实 Electron 界面报告](ui-report.json)、[429 等待截图](retry.png)、[问题草稿窄窗口截图](question-narrow.png)。Browser plugin not available，使用 Playwright Electron 启动独立 userData 的真实仓库主进程与最终 dist。所有开始、输入、刷新、提交、批准、展开和导航均通过可见界面；本机模拟服务仅提供模型响应。仅关闭自己测试实例，没有操纵日常应用或 OpenAI 窗口。3 次请求包含一次 429，未将缺失用量记零。
- 长任务脚本：tests/team-long-lifecycle.test.cjs 与 fixtures/team-long-worker.cjs。由两独立 Node 进程运行生产 TeamRuntime/runAgent/ElectronTransport、执行守卫、operation journal 和 durable store；进程在 started 已落盘而 completion 未落盘时被强制终止。7 次压缩通过生产验证/持久化路径，摘要由夹具生成。用户纠正只在真正补充后进入后续请求。初次重载 claim 拒绝；第一次核实后账本仍返回未知，再次明确核实才派发。成功副作用计数始终为 1。每次记录预算累计，重启不清零；不是 Electron IPC、供应商语义或断电验收。
- [真实三策略报告](strategy/README.md)、[匿名逐请求数据](strategy/anonymous-report.json)、[离线校验](strategy/verification.json)、[当时引擎哈希](strategy/source-manifest.json)。原始合成题由固定答案校验器独立评分；三次运行保留在 waiting_user，未把程序评分代作用户验收。
- [轻度改写固定样本结果](light-rephrase-report.json)：15 例、无误报、1 例漏报；这些是工程固定样本，不是外部盲测。提示不独立停止/延长任务，loopGuard=false 仍受尊重；代码块、数值变化与正常新证据边界另有回归。
- 记忆与 Skill 的结构/注入固定题位于 tests/team-memory.test.cjs、tests/skill-folding.test.cjs；[真实免费模型四例注入对照](memory-skills/README.md)、[匿名响应与用量](memory-skills/anonymous-report.json)、[离线核验](memory-skills/verification.json)均通过；总计 1,207 供应商实报 tokens，不与结构测试混为同一证明。

最终构建还复验了上一批文件交付闭环：[文件任务 UI 回归](files-ui-report.json)，修改/复核/差异合并、只读、命令批准与完成后重启均通过。运行摘要优先显示执行成果，复核协议详情仍在步骤证据中。

## 复现与限制

工作区 wickrun-engineering-21710 保存独立 UI 启动/验收脚本。三策略重跑脚本 scripts/run-team-strategy-comparison.cjs 会使用现有授权免费接入且产生真实模型请求；离线验证脚本无需联网。实际模型报告不包含凭据、请求头或日常项目内容。

安装升级、原生目录对话框、新手完成率、420 px/深色/英文全套界面、真实供应商长摘要语义、复杂题策略收益与通用语义停滞仍不在通过范围。无需打包即可完成本轮源码测试；验证已安装应用的新行为和迁移时才需要打包安装。
