# 2.17.6 项目进展验收

工程基线：`74cab88 / 2.17.5`。用户已确认发布并安装；本轮只读提取已安装 `app.asar/package.json`，版本为 **2.17.5**。交付源码版本 **2.17.6**，本轮未提交、打包、安装或发布。

## 完成范围

U0c-a：总览按实际记录展示探索中、待你决定、待执行、执行中、已完成、需要处理，附当前原因和对应任务/运行入口。需要处理的任务优先，其次待决定；可按阶段筛选。梳理完成与实际交付完成分别说明；原始想法、当时建议和已创建的后续任务互相可追溯。

状态投影不信任旧 `task.status`：有实际活动运行时不会被更新的空运行遮住；另一条完成记录不会消除未核实或暂停记录；旧探索结果不会覆盖新轮失败；未经过交付确认不算完成。恢复后的当前运行只显示当前尝试的提示，不重放历史错误作为等待原因。来源关系不触发运行，也不是执行依赖。

## 工程检查

- [完整回归日志](automated-tests.txt)：**702/702**，无失败、跳过或取消。本批增加 18 个阶段/来源/状态回归；国际化检查覆盖组件和纯状态模块。
- [构建日志](build.txt)：TypeScript 检查与生产构建通过。既有主包超过 1200 kB 的提示仍存在。
- `git diff --check` 通过；版本同步到 package.json、package-lock 两处、APP_VERSION 和两份 README 徽章。
- Sol 在隔离目录实现纯状态模块和回归，根代理审查、集成并补充修正；二次只读审查发现旧错误可能混入恢复后的当前提示，已修复并新增回归。

## 实际界面验证

原生 Windows 控制工具无法读取/激活日常安装版，重新选择更新后创建的窗口仍得到 `foreground window did not report a process id`。未在本轮通过它完成真实免费路由试验。浏览器技能未提供；依前端测试流程使用现有 Playwright 的 Electron 接口启动独立应用实例，加载最终生产构建，点击真实产品 UI。使用独立 userData、8 项合成任务、5 份合成运行记录，无模型凭据，不操作日常项目。

| 项目 | 结果 |
|---|---|
| 页面身份、非空、框架错误层、控制台 | 实际 dist/index.html 与 wickrunAI 标题匹配；8 项任务可见；无页面异常和相关控制台错误 |
| 阶段筛选 | 待你决定同时包含探索结果和实际交付验收；旧任务文字不影响计数 |
| 准确的下一步 | 从待决定进入对应交付确认；从需要处理进入对应中断核实页 |
| 来源与后续任务 | 从梳理完成项进入后续任务，再返回原想法；原建议链接进入精确的冻结探索运行 |
| 缺失来源 | 任务缺失、运行缺失或不匹配均明确显示，不构造虚假的有效关系 |
| 键盘与空态 | Space 激活阶段按钮；空阶段可返回全部任务 |
| 刷新 | 重新加载后 8 项任务与阶段仍存在 |
| 布局 | 1280×800 与 720×800 检查通过；窄屏总览 left=18、right=692、scrollWidth=clientWidth=674 |
| 导航不执行 | 结束后仍为 8 项任务、5 份运行；中断夹具按既有恢复逻辑标记 uncertain |

首个 UI 试验的合成完成记录遗漏结束节点批准，产品持久层拒绝导航时的保存。修正新测试目录的初始夹具后重跑通过；没有放松产品完成校验。窄屏首张截图抓在既有侧栏收起动画中途，导致左边暂被覆盖；测试改为等待抽屉完全离开，再检查左右边界，最终截图通过。这两项都保留为测试方法调整，未宣称产品发生相应功能缺陷。

证据：[UI 报告](ui-report.json)、[源码与构建摘要](source-evidence.json)、[总览](project-overview.png)、[来源关系](source-links.png)、[窄窗口](project-overview-narrow.png)。测试脚本和独立数据位于工作区 `wickrun-engineering-2176`；其行为是 UI 导航，未调用产品隐藏状态修改接口。

## 仍待完成

- **U0b**：真实免费模型的模糊想法、信息不足、改变主意及无效建议四类试验，和首次使用者独立操作。[记录规范](REAL_PLANNING_PROTOCOL.md)已建立，但本轮没有真实样本，不能声称已降低真实学习成本。
- **U0c-b**：多个可编辑建议、原子选择保存、持久依赖、前置产物绑定、运行器与主进程派发约束、真实任务依赖图。当前来源关系不支持这些能力。
- 真实长任务进程重启、三策略质量/成本对照、客户端证据桥、记忆与 Skills 收益和系统沙箱仍维持待验收。此次启动恢复的合成记录不等于实际长任务崩溃续跑证明。

未测试的界面：最小 420 px 桌面窗口、英文长文案及深色模式的视觉效果；翻译词条覆盖已通过，但不替代这些视觉验收。
