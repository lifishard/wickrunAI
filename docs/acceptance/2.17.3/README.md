# 2.17.3 验收记录 · 2026-09-20

安装版基线为 2.17.2 / ecc5578，本批源码为尚未提交、打包、安装或发布的 2.17.3。实际操作了已安装应用和独立的 2.17.3 Electron 验收窗口，没有关闭日常应用或 OpenAI 窗口。

## 已通过范围

- 同一复核连续失败、内容/要求相同且无新证据时停止自动返工。文件和文本规范化、变化时继续、无法规范化时沿用原次数上限均有自动化覆盖；不推断语义等价。
- 新用户补充指令在模型声明要求前即纳入比较，防止把新要求误判成停滞。
- 真实桌面：新建运行 → 执行/复核各两次 → 待核实 → 重载窗口 → 明确重试 → 仅新增复核一次 → 再次停滞 → 人工接受 → 最终交付确认 → 批准完成。
- 最新运行 `teamrun-53cf3346-6b4d-4bdd-ad2f-440502f335da`：2 次执行、3 次复核、5 个唯一请求；本机合成 HTTP 服务每次报告 200 tokens，共 1000。重试前 800、重试后 1000；没有第三轮执行。复核判词仍为 fail，最终完成是人工接受测试结果。
- 646/646 自动化、类型检查和生产构建通过；保留既有大包警告。普通中断/讨论接续回归也在全量测试中通过。

桌面初测发现旧检查点内容与新 JSON 拼接。修复用持久停滞标记限定“重新生成”，保留旧 memberStates/memberOutputs 和请求统计；普通未知副作用恢复不受影响。持久化测试加入真实结构的 content/spentTokens 后先复现失败，修复后通过，再使用真实 runAgent 的桌面链路确认。最初一轮夹具误启用了文件工具，完成检查正确拦截；该配置错误和随后发现的产品重试错误均保留在历史运行中，没有清除失败记录。

## 安装版免费路由试验

通过 2.17.2 界面创建项目 `2.17.2 免费路由验收`，导入同题三策略配置；本轮只执行单成员版本，三个不同路由各建一个运行。固定输入是 CSV 最新修订再过滤题，独立校验期望 included_ids 为 r2/r3/r4、total 为 1250。输入文件均未改动，没有生成 report.json。

| 授权入口 | 实际结果 | 结论边界 |
|---|---|---|
| SenseNova deepseek-v4-flash | 1 次 HTTP 429，inference exceeds tpm/rpm limit / insufficient_quota | 当次固定模型请求失败 |
| omni auto/coding:free | 1 次 HTTP 403；网关解析为 oc/big-pickle，供应商限制仅供其客户端 | 没有直接选择 oc/ 路由 |
| OpenRouter qwen/qwen3.8-27b:free | 3 次 HTTP 429 后通过应用暂停，保留待核实状态 | 没有继续无限重试 |

自动接力关闭，未直接使用 dva/ 或 oc/；5 个请求没有返回用量，记录为缺失，不能认定免费额度可用、零费用或整个平台不可用。讨论后执行及执行后复核没有真实供应商结果，三策略收益比较尚未开始。

## 证据

- [完整自动化日志](automated-tests.txt)、[构建日志](build.txt)、[安装版试跑数据](installed-free-pilot.json)。
- [桌面持久记录与 HTTP 核对](desktop-evidence.json)：包含本次尝试、冻结文本、判词、恢复依据、逐请求身份/用量、源码哈希和早期失败摘要。
- [修复前重试失败](retry-before-fix.png)、[修复后两轮停滞](stagnation-fixed-stop.png)、[重载后单次重试再次暂停](retry-after-fix.png)、[最终交付门槛](delivery-gate.png)、[批准完成](completed.png)。
- 只读复核工具 [verify-desktop.cjs](verify-desktop.cjs)：传入隔离 userData 目录和对应合成 HTTP 日志；验证成功才输出报告，不写入应用数据。

本地夹具和隔离数据位于工作区 `wickrun-engineering-2173`。本次只重载渲染窗口，没有关闭/重启 OS 应用进程；实际进程重启与真实模型语义质量仍待验收。文件型停滞本轮通过自动化，新增桌面验收覆盖的是纯文本型。

## 后续出口

先选择另一个授权免费固定模型完成单任务产物验收，再在同一可用模型与预算下跑完整三策略；供应商继续受阻时推进订阅客户端证据桥。真实长任务组合、记忆与 Skills 的质量对照、语义停滞、价格快照及系统级隔离仍按路线图逐项推进，本报告不将其标记完成。
