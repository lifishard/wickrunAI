# 协作空间易用性支线

## 2026-09-20 第十一批 2.17.10：等待恢复、长任务组合与策略质量验收

基于最新未提交源码继续，保留此前改动；Git 基线仍为 74cab88 / 2.17.5。当前源码 **2.17.10**，未提交、打包、安装或发布。本轮按用户限定的四项推进，代码与独立应用验收无需重新打包。

| 本轮事项 | 已实现与已验证 | 结论边界 |
|---|---|---|
| U4 等待、恢复；U5 导航 | 运行页先显示状态、原因、下一步与已有产出；按真实等待状态显示重试倒计时。问题卡支持草稿落盘、刷新恢复、有效回答后从原问题继续。流程图/证据/事件按需展开；默认导航为总览、任务、文件，其他设置可展开。独立 Electron 跑通真实 HTTP 429→等待→问题→刷新→回答→交付批准。 | 模型响应由本机夹具生成；没有声称新手完成率改善已经测定。 |
| 长任务组合 | 同一运行含中途纠正、7 次生产压缩流程、两条本机路由切换、原始证据 read_context 取回、真实子进程强制中断与重载。已完成副作用后再次请求仅执行一次；未知结果先拒绝恢复，再经明确核实才能重试。 | 压缩摘要和模型决策为确定性夹具；真实生产运行器、账本、文件存储与 HTTP 管道。不是机器断电、真实供应商摘要语义或流式 UI 验收。 |
| 单助手与多助手 | 固定免费 SenseNova deepseek-v4-flash、同题、相同 60k 总/20k 阶段预算：单助手 3,832 tokens/5.759s；讨论后执行 11,393/20.103s；执行后复核 10,860/42.651s。三者程序答案全对，误报完成均 0，文本复核 pass。 | 每策略仅一次确定性题；不推导普遍质量排名或金额节省。证据使用整合前 2.17.9 引擎快照，保留源码哈希；不为版本号重发模型请求。 |
| 记忆、Skills 与循环 | 记忆提示保留版本/证据来源并明确当前要求优先；停用 Skill 不注入、不折叠提供、不允许 read_skill 取回。固定启用/停用、撤销/过期、范围、冲突提示测试通过。轻度改写从相似度硬停调整为保守提示，只有原完成检查已要求继续时才附加，不独立停机或增加轮次。15 固定样本：TP 3/TN 11/FP 0/FN 1。 | 检索与注入检查不等于模型普遍正确采用。保留 tp-cn-review-light-paraphrase 漏报，不宣称全面语义识别。真实免费模型四例单轮对照均通过：缺信息为 unknown、有效经验提供正确事实、当前纠正覆盖旧经验、撤销/过期与停用内容排除；不代表普遍质量。 |

回答入口同时补齐两项保护：中断后还处于 running 的非问题工具也视为待核实，即使尚未落下 uncertainCallId；并行复用同一助手时，问题句柄按运行/尝试/成员三者隔离。回答不能充当对未知操作的批准。

验证：**789/789 全量回归**、正式类型、132 文件快速检查、生产构建通过；最终整合构建再次完成独立 Electron 界面验收。证据见 [2.17.10 验收](acceptance/2.17.10/README.md)。既有主包体积提示保留。

本轮四项按上述有界范围完成。仍保留的扩大验收：真实首次使用者、复杂任务与重复采样、多次压缩的真实模型语义、操作系统级隔离及安装升级。它们不被这一小样本自动标记通过。若验证日常安装版的新行为与旧数据升级，下一步才需要打包安装 **2.17.10**；不要把已安装旧版当作本轮源码。

以下旧批次为历史记录，当前状态以上文为准。

用户在 2026-09-20 明确选择：**任务优先，提供可展开的高级设置**。目标同时包含降低首次使用难度和提高可定制性；只隐藏按钮不算完成。

## 2026-09-20 第十批 2.17.9：文件任务首跑与实际保存故障修复

Git 基线仍为 **74cab88 / 2.17.5**，保留此前未提交改动；当前源码 **2.17.9**。本轮未提交、打包、安装、同步 GitHub 或发布。源码施工与独立 Electron 验收不需要用户先打包。

- **U3 新文件任务入口已实现**：在任务页选“处理文件”、目录及只读/修改能力；命令按需展开并显式启用。自动准备独立助手和真实流程，开始前显示本次范围。支持直接完成或独立复核；只读分析采用文本复核，实际修改采用文件产物复核。
- **选择约束实际执行**：目录与能力冻结在任务、成员来源及运行中；选择第二个目录不会落回第一个。任务保存、运行器和主进程分别检查。新文件团队不能转借给无范围或不同范围的任务；页面过滤不匹配流程，后端仍拒绝绕过。复制模型/职责与保存团队方案不携带目录或工具授权。旧无范围成员与运行保持兼容。
- **批准与交付保持明确**：即使项目为“全部批准”，本入口仍逐项确认修改和命令；复核助手只读。原目录只在用户检查差异并明确合并时更新。只读任务不发布空文件产物。修正复核列目录工具名称，并显示本步骤实际启用的工具数量。
- **实际应用发现并修复保存故障**：Windows 短暂占用导致进度 JSON 原子 rename 返回 EPERM。增加同一次保存累计最多 250 ms 的有限重试，保留原子替换；每次重试重新检查主文件哈希，外部修改或持续失败不覆盖。保存失败显示原始原因，不再被暂停回调掩盖。
- **完成判断使用实际目标**：文件任务将真实目标/验收与系统协议说明分开传给完成检查，识别“不得修改”等否定；真实读取仍需成功证据。修改和测试任务的对应证据闸门保留，避免为了通过只读任务而关闭质检。
- **验证**：772/772 全量、56/56 针对性、正式类型检查、快速检查及生产构建通过。实际独立 Electron 完成修改→写入批准→文件复核→用户验收→差异预览→合并、只读任务、显式命令批准与真实本机命令、720 px 布局、完成后进程重启。外部真实模型与新手效果不归入本轮通过结论。证据见 [2.17.9 验收](acceptance/2.17.9/README.md)。

**下一工程项：U4 等待与恢复说明，接续 U5 导航和新手体验**。U0b 真实免费模型/首次使用者、长任务组合、三策略收益、客户端证据、记忆/Skills 质量及系统隔离继续保留；下方旧版本段落为历史状态。

当前范围：新文件任务的直接执行/独立复核；不在这个入口新增文件讨论、跨任务文件依赖或任意复杂流程改造。目录选择使用既有桌面选择器，实际 UI 测试选择预置项目的第二个目录，未操纵系统原生目录对话框。真实免费模型、首次使用者、执行中崩溃、英文/深色/420 px 仍待验收。后续工程顺序 U4 → U5；其必要性和最小出口见 NEXT_STAGE_ACCEPTANCE.md 顶部。

## 2.17.8：按角色定制与保存团队方案

U2 的新文本任务准备范围已实现。默认入口不要求配置角色；展开后可分别选择执行/讨论/复核、方向探索/方案整理的接入、模型及职责，也可复制已有成员的模型与职责。统一模型会同步全部角色，个别选择可随后修改；流程预览显示实际配置。复制为本任务独立成员，原成员与已创建运行不改写。

项目内最多保存 20 个团队方案，规范化重名拒绝；载入后可用新名称另存，删除只删方案。方案不带任务目标、验收、凭据、工具/技能或自动换路由。直接准备与保存后载入准备使用相同的默认思考强度/额度，避免隐藏继承造成差异。探索职责编辑只显示自然语言，实际节点仍强制保留结构化建议要求；复核保留引用证据协议。

验收结果：740 项自动化及独立 Electron 双角色文本闭环通过。角色模型与职责确实进入两个独立本机请求；保存/载入/准备不发请求，刷新与完成后进程重启保留方案；另存、删除和第二任务准备不改变旧成员及旧运行。1280/720 px 布局通过。证据见 [2.17.8](acceptance/2.17.8/README.md)。

此项尚未扩展到已有任意流程的就地重配、跨项目方案分享或文件能力设置；复杂流程保留原高级入口。真实新手、真实免费模型、英文/深色/420 px 窗口未验收。下一工程项 **U3 文件任务首跑**，先明确目录和能力，再准备隔离任务；不得把文本配置成功当作文件交付成功。

## 2.17.7：多任务建议和真实文本依赖

U0c-b 的文本范围已实现并完成独立 Electron 验收。下方 2.17.6 中“待施工”是历史状态，以本节为准。

用户从可读建议进入逐项选择与编辑，按需展开先后关系；关闭可恢复，放弃可清除。保存只创建任务，开始时再选助手与工作方式。重复/缺失/自依赖/循环、漏选前置、同组互斥方向和过大方案均拒绝。旧单项建议及其历史已采用任务保持兼容。建议不能指定工具或授权。

总览关系图来自真实保存的任务依赖。后续任务需等待所有前置的最新运行完整结束并经用户验收；准备时可展开看输入，保存运行时冻结源运行和实际文本。前端运行器与主进程都检查，后续请求确实收到输入。每任务最多 8 个直接前置，总文本最多 32,000 字符，超限不截断；含文件产物则拒绝。当前按项手动开始，原有并发和预算限制仍生效。

| 验收条件 | 本轮结果 |
|---|---|
| 编辑、选择、互斥方向、循环、重复采用与原子保存 | 自动化通过；编辑/遗漏前置/保存通过实际 UI |
| 草稿刷新恢复、明确放弃 | 实际 UI 通过；放弃持久删除，不更改任务与原建议 |
| 保存不执行、前置未验收不派发 | UI 与运行/HTTP 数量核对通过 |
| 前置产出实际传入，并绑定精确来源 | 两任务独立 UI 开始/验收及本机 HTTP 记录通过 |
| 新的前置运行或目标修改不替换旧输入 | 运行器与主进程回归通过 |
| 关系图、窄窗口、完成后重启 | 实际 Electron 通过，720 px 无关系区横向溢出 |
| 执行中崩溃、多个窗口同时抢跑 | 本轮未做实际桌面组合试验，不以完成后重启代替 |
| 真实模型输出稳定性、首次使用者理解 | 尚未验收 |

证据见 [2.17.7](acceptance/2.17.7/README.md)。下一工程项 **U2**：可读步骤旁分别换模型/职责，复用已有成员；必须预览最终实际图，开始后旧运行仍保持原成员快照。之后 U3 文件任务首跑、U4/U5 恢复与导航。U0b 真实情境、新手验证及主线待办继续保留。

## 2.17.6：阶段总览与后续任务关系

以用户已发布、安装的 2.17.5 为基线。本轮把 U0c 拆成可分别验收的两批：

- **U0c-a 阶段总览**：总览显示探索中、待你决定、待执行、执行中、已完成和需要处理；每项任务给出当前原因及进入准确任务/运行的下一步。按实际运行记录判断，旧 task.status 不作为成功证据；梳理完成与实际交付分别说明。来源任务、原建议运行和已保存后续任务均可跳转，丢失或不匹配的来源有提示。它们只说明来源，不是运行依赖。
- **U0c-b 多任务建议与执行依赖，待施工**：允许在创建前选择、编辑多个建议和依赖。先实现建议格式及整体校验，再实现持久依赖和运行约束，最后接入可读项目关系图，避免 UI 提前声称系统能够自动编排。

U0c-a 验收：阶段筛选与空态；待验收不会误报完成；继续探索时旧建议不会覆盖新轮失败；旧的未核实运行不会因另一轮完成而消失；活动运行不会被新的空运行覆盖；来源跳转准确、历史缺失明确；重新加载结果一致。实际通过情况和限制见 [2.17.6 验收记录](acceptance/2.17.6/README.md)。

U0b 保持优先验收但本轮仍未通过。原生桌面控制失败，不据此推断免费模型不可用。已建立[四类情境与首次使用者记录规范](acceptance/2.17.6/REAL_PLANNING_PROTOCOL.md)，隔离实例的合成记录不算真实模型或新手结果。

下一施工顺序：恢复可操作条件后补 **U0b 真实试验**；工程继续 **U0c-b → U2 → U3 → U4/U5**。U0c-b 的具体出口：

1. 一次规划可以建议若干独立或有先后关系的任务；区分互斥备选方向与应同时创建的工作项。模型只能建议文本和同组依赖，不得植入工具、模型接入、成员或授权。
2. 用户逐项编辑和选择后才原子保存；拒绝重复编号、缺失依赖、自依赖、循环、未选择的前置任务及过大方案。来源保留到原始想法和实际规划运行；创建任务不发请求。
3. 运行器及主进程持久层共同拒绝未满足依赖的派发。准备运行时绑定已验收前置运行与实际产出证据，续跑不随新方案漂移；取消、失败或只有文字建议的前置项不满足条件。
4. 项目关系图显示真实已创建任务和明确依赖，给出哪些可继续、哪些在等待哪个产出；并行仍受项目并发和预算限制。依赖产物必须确实进入后续任务输入，不能只解除按钮禁用。
5. 验收覆盖前置未完成、部分完成、重新规划、刷新/重启、并发抢跑、历史迁移及取消路径，并从产品界面完成至少一组先后任务。未完成运行约束之前，不将来源连线描述成自动执行图。

## 2.17.5 增补：直觉优先的起点与流程理解

用户最新要求：允许从 vague idea 开始；不要先要求用户构思清楚 workflow；选已有流程必须解释具体发生什么。沿用应用现有视觉风格，调整信息顺序和操作路径。Impeccable 的 onboarding/distill 原则用于降低前置概念负担，代码和桌面行为是验收依据。

已完成 U0a：一句想法 → 两位助手探索与整理 → 可读的方向/问题/建议 → 补充讨论，或编辑后保存独立执行任务。已完成 U1b：从实际图和已保存版本生成示意，能查看负责人、指令、产出与所有去向；返工、分支和草稿差异不隐藏。建议只带入文本，不成为自动执行授权。

验收条件及结果：

| 条件 | 本轮结果 |
|---|---|
| 空白任务只填一句想法，不要求名称、具体计划或完成标准 | 受控桌面通过，应用重启后原始想法保留 |
| 准备前后都能看懂下一步，准备本身不派发 | UI 与持久记录通过，准备后 run/request 均为 0 |
| 补充讨论保留上一轮语境，上一轮不会占住新轮运行名额 | 桌面先复现阻塞后修复；运行器/存储回归与再次桌面均通过 |
| 建议可修改，建立新任务前后不自动派发 | UI 修改目标并保存通过，来源可追溯 |
| 已有流程显示实际运行版本、真实分支与负责人 | 桌面选择带返工的复核流程通过；草稿另有修改提示可见 |
| 任意分支/并行/循环不会被预览伪装成线性顺序 | 纯图测试通过；本轮桌面重点为顺序探索与复核返工图 |
| 对 AI 不了解的用户能自行完成并解释流程 | **待真实新手测试**；开发者操作和模拟模型不能替代此项 |

下一阶段顺序（覆盖下方旧 U2→U5 排序）：

1. **U0b 真实规划可用性与新手验证**：只用用户允许的免费入口，验证模糊想法、信息不足、改变主意、无效建议四类情况；记录问答轮数、错误假设、帮助请求和用量缺口。至少让首次使用者从一句话走到可执行的下一任务，并能解释每一步做什么。尚不能声称已降低真实学习成本。
2. **U0c 项目阶段总览与多任务建议**：先展示“探索中/待你决定/待执行/执行中/已完成”的项目关系；多个任务建议带来源和明确依赖，逐项修改、选择后才建立实际任务。验收：用户不用打开设计器就能知道项目目前在哪、下一步是什么、哪些可并行。不能把文字计划装饰成已实现的执行图。
3. **U2 按角色定制与复用**：在可读步骤旁换助手/模型、修改职责、选择已有成员并保存团队预设；真实图与冻结运行保持一致。
4. **U3 文件任务首跑**：按目标引导选目录与能力，说明会读取/修改什么；仍走隔离副本及实际合并验收。
5. **U4 运行状态与恢复、U5 导航整体收敛**：先呈现当前产出、等待原因和可执行下一步；技术细节按需展开。用新手数据决定继续改哪里。

上述优先级不会取消主线真实三策略对照、实际进程重启续跑、客户端证据、记忆/Skills质量与沙箱能力的待验收项。

## 已观察的问题

- 首次运行原先要求用户先配置成员、生成流程、进入设计器保存版本，再回到任务启动。
- 任务页并列三个生成按钮、流程选择、运行前检查；按钮讲实现方式，用户仍需自行决定先点哪个。
- 成员、工作流、节点、版本等概念先于任务出现，而“谁执行、谁检查、用哪个模型、能操作什么”分散在不同页面。
- 限流有实际等待机制，但配额错误分类会让部分请求提前停下；失败记录需要区分自动等待、人工暂停和不可重试。

## 第一批 U1：文本任务快速准备（2.17.4）

保留现有界面风格和执行器，在任务页提供模型接入、模型和工作方式。三种方式为直接完成、讨论后完成、完成后复核；默认直接完成，并明确说明额外模型调用。

准备操作创建普通成员、可编辑工作流和固定版本，下一步展示运行前确认；准备本身不发模型请求。新成员只处理文本、关闭自动接力、预算收敛到项目上限。原有成员、流程、目录、权限和历史运行不改写。已有流程仍走原入口；文件流程、成员配置和已有流程选择收进可展开的高级设置。

验收条件：

- 已配置 API 的空项目，无需访问成员页或设计器即可启动文本任务。
- 从保存任务到派发只需“准备并检查”和“确认范围并开始”；省略的是手工配置，不省略运行确认。
- 准备后有 1/2 位成员、1 份流程和 1 个固定版本，运行数与请求数仍为 0。
- 三种方式分别生成正确结构；所有结构保留人工交付门槛；旧团队和预算/允许接入规则不被绕过。
- 高级入口可找到原有能力，生成的流程仍可修改；不能成为不可定制的黑盒。
- 关闭自动重试时如实提示；开启时说明默认 62 秒及 Retry-After 优先，不把所有 429 视为永久额度不足。

工程与桌面证据集中在 `docs/acceptance/2.17.4/`。本批只覆盖文本快速入口，不宣称已经完成文件任务、混合客户端或完整学习成本评估。

## 后续施工顺序

| 项目 | 具体工作 | 验收出口 |
|---|---|---|
| U2 · 可直接理解的定制 | 在任务页分别选择执行/讨论/复核模型，编辑角色职责；可选择已有成员，避免每次重复配置；提供团队预设保存/复制 | 调整一位成员不必学习节点；运行前能看到角色→模型→权限映射；旧运行冻结配置保持不变 |
| U3 · 文件任务首跑 | 在任务页选择工作目录和所需能力，提供文件任务的准备路径；明确读取、修改、命令的区别 | 首次文件任务不必跨页找目录/工具；能力与实际派发一致，修改仍在隔离区，未授权能力不可执行 |
| U4 · 运行页与恢复 | 优先展示任务进度、当前等待原因和下一步行动；节点图、调用编号及调试细节按需展开；区分等待重试、待回答、待批准、待核实 | 不看流程图也能判断为何停住；暂停可用；恢复不会重复已成功的副作用 |
| U5 · 导航与新手验证 | 收敛默认导航，统一成员/流程术语；改善表单说明、草稿恢复和连接缺失时的下一步 | 请首次使用者独立完成文本、文件、复核三类任务，记录完成率、求助次数、误操作和时间，再与原入口比较 |

U2/U3 提升定制的可发现性与复用，U4/U5 降低理解运行状态的成本。主线的真实模型质量、恢复与证据工作继续保留；本支线不以界面简化替代其验收。
