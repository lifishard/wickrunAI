# wickrunAI 工程施工路线图

## 2026-09-20 第十一批 2.17.10：等待恢复、长任务组合与策略质量验收

基于最新未提交源码继续，保留此前改动；Git 基线仍为 74cab88 / 2.17.5。当前源码 **2.17.10**，未提交、打包、安装或发布。本轮按用户限定的四项推进，代码与独立应用验收无需重新打包。

| 本轮事项 | 已实现与已验证 | 结论边界 |
|---|---|---|
| U4 等待、恢复；U5 导航 | 运行页先显示状态、原因、下一步与已有产出；按真实等待状态显示重试倒计时。问题卡支持草稿落盘、刷新恢复、有效回答后从原问题继续。流程图/证据/事件按需展开；默认导航为总览、任务、文件，其他设置可展开。独立 Electron 跑通真实 HTTP 429→等待→问题→刷新→回答→交付批准。 | 模型响应由本机夹具生成；没有声称新手完成率改善已经测定。 |
| 长任务组合 | 同一运行含中途纠正、7 次生产压缩流程、两条本机路由切换、原始证据 read_context 取回、真实子进程强制中断与重载。已完成副作用后再次请求仅执行一次；未知结果先拒绝恢复，再经明确核实才能重试。 | 压缩摘要和模型决策为确定性夹具；真实生产运行器、账本、文件存储与 HTTP 管道。不是机器断电、真实供应商摘要语义或流式 UI 验收。 |
| 单助手与多助手 | 固定免费 SenseNova deepseek-v4-flash、同题、相同 60k 总/20k 阶段预算：单助手 3,832 tokens/5.759s；讨论后执行 11,393/20.103s；执行后复核 10,860/42.651s。三者程序答案全对，误报完成均 0，文本复核 pass。 | 每策略仅一次确定性题；不推导普遍质量排名或金额节省。证据使用整合前 2.17.9 引擎快照，保留源码哈希；不为版本号重发模型请求。 |
| 记忆、Skills 与循环 | 记忆提示保留版本/证据来源并明确当前要求优先；停用 Skill 不注入、不折叠提供、不允许 read_skill 取回。固定启用/停用、撤销/过期、范围、冲突提示测试通过。轻度改写从相似度硬停调整为保守提示，只有原完成检查已要求继续时才附加，不独立停机或增加轮次。15 固定样本：TP 3/TN 11/FP 0/FN 1。 | 检索与注入检查不等于模型普遍正确采用。保留 tp-cn-review-light-paraphrase 漏报，不宣称全面语义识别。真实免费模型四例单轮对照均通过：缺信息为 unknown、有效经验提供正确事实、当前纠正覆盖旧经验、撤销/过期与停用内容排除；不代表普遍质量。 |

回答入口同时补齐两项保护：中断后还处于 running 的非问题工具也视为待核实，即使尚未落下 uncertainCallId；并行复用同一助手时，问题句柄按运行/尝试/成员三者隔离。回答不能充当对未知操作的批准。

验证：**789/789 全量回归**、正式类型、132 文件快速检查、生产构建通过；最终整合构建再次完成独立 Electron 界面验收。证据见 [2.17.10 验收](acceptance/2.17.10/README.md)。既有主包体积提示保留。

本轮四项按上述有界范围完成。仍保留的扩大验收：真实首次使用者、复杂任务与重复采样、多次压缩的真实模型语义、操作系统级隔离及安装升级。它们不被这一小样本自动标记通过。若验证日常安装版的新行为与旧数据升级，下一步才需要打包安装 **2.17.10**；不要把已安装旧版当作本轮源码。

以下旧批次为历史记录，当前状态以上文为准。

## 2026-09-20 第十批 2.17.9：文件任务首跑与实际保存故障修复

Git 基线仍为 **74cab88 / 2.17.5**，保留此前未提交改动；当前源码 **2.17.9**。本轮未提交、打包、安装、同步 GitHub 或发布。源码施工与独立 Electron 验收不需要用户先打包。

- **U3 新文件任务入口已实现**：在任务页选“处理文件”、目录及只读/修改能力；命令按需展开并显式启用。自动准备独立助手和真实流程，开始前显示本次范围。支持直接完成或独立复核；只读分析采用文本复核，实际修改采用文件产物复核。
- **选择约束实际执行**：目录与能力冻结在任务、成员来源及运行中；选择第二个目录不会落回第一个。任务保存、运行器和主进程分别检查。新文件团队不能转借给无范围或不同范围的任务；页面过滤不匹配流程，后端仍拒绝绕过。复制模型/职责与保存团队方案不携带目录或工具授权。旧无范围成员与运行保持兼容。
- **批准与交付保持明确**：即使项目为“全部批准”，本入口仍逐项确认修改和命令；复核助手只读。原目录只在用户检查差异并明确合并时更新。只读任务不发布空文件产物。修正复核列目录工具名称，并显示本步骤实际启用的工具数量。
- **实际应用发现并修复保存故障**：Windows 短暂占用导致进度 JSON 原子 rename 返回 EPERM。增加同一次保存累计最多 250 ms 的有限重试，保留原子替换；每次重试重新检查主文件哈希，外部修改或持续失败不覆盖。保存失败显示原始原因，不再被暂停回调掩盖。
- **完成判断使用实际目标**：文件任务将真实目标/验收与系统协议说明分开传给完成检查，识别“不得修改”等否定；真实读取仍需成功证据。修改和测试任务的对应证据闸门保留，避免为了通过只读任务而关闭质检。
- **验证**：772/772 全量、56/56 针对性、正式类型检查、快速检查及生产构建通过。实际独立 Electron 完成修改→写入批准→文件复核→用户验收→差异预览→合并、只读任务、显式命令批准与真实本机命令、720 px 布局、完成后进程重启。外部真实模型与新手效果不归入本轮通过结论。证据见 [2.17.9 验收](acceptance/2.17.9/README.md)。

**下一工程项：U4 等待与恢复说明，接续 U5 导航和新手体验**。U0b 真实免费模型/首次使用者、长任务组合、三策略收益、客户端证据、记忆/Skills 质量及系统隔离继续保留；下方旧版本段落为历史状态。

## 剩余任务为什么还需要做（2.17.9 排序）

这些任务解决不同问题，并不是把同一套功能换几个名字。上下文与恢复负责“继续做对”，证据与质检负责“知道做对没有”，易用性负责“用户能开始、理解和接手”。已有底层能力也不等于真实场景已验收。

| 顺序 | 剩余工作 | 必要性：不做会留下什么问题 | 最小验收出口 |
|---|---|---|---|
| 本批已完成（原生目录选择对话框尚未实测） | U3 文件任务入口与冻结范围 | 文本入口已经简单，但文件任务仍要跨页找目录、工具和流程；只简化页面而不限制执行，会造成界面与实际权限不一致。 | 任务页选目录/能力，运行实际使用所选目录；只读不能写，复核只读，修改隔离，查看差异后人工合并。 |
| 下一工程批次 | U4 等待、恢复与 U5 导航说明 | 429 退避已共用执行器，但用户仍可能分不清“正在等模型”和“需要自己处理”，进而反复开始、错误重试或找不到验收入口。 | 不看节点图能解释当前状态、等待原因、下一步；区分重试/回答/批准/核实；未知副作用不能自动重发。 |
| 优先真实验收 | U0b 免费模型与首次使用者 | 合成回复可以证明按钮和状态接通，无法证明模型能从模糊想法形成好建议，也无法证明初学者看得懂。 | 模糊想法、缺少信息、中途改意、无效建议四类实测；首次使用者独立做文本/文件/复核，记录求助、误操作、耗时。仅使用已授权免费路由。 |
| 高优先可靠性 | 长任务：多次压缩、改要求、换模型与执行中重启组合 | 单项保护已实现，真实组合仍可能丢失否定要求、沿用旧结论或重复成功操作。完成后重启不能替代执行中崩溃。 | 同一任务包含纠正、至少两次压缩、跨接入切换和真实进程中断；逐条追溯原证据，成功动作不重复，未知结果显式核实。 |
| 多助手收益验证 | 固定免费模型、同任务三策略对照 | 多助手增加步骤与等待，不一定提高质量；没有对照就无法判断何时值得推荐讨论或复核。 | 同题/模型/预算比较单助手、讨论后执行、执行后复核；外部检查正确率、误报完成、耗时、tokens，不用模型自评分代替答案。 |
| 按客户端需求推进 | 订阅客户端证据桥 | API 成员的工具记录不能自动覆盖 Claude/Codex 客户端；仅凭“已经完成”的回复无法确认文件、测试及版本。 | 把可取得的真实工具/文件/测试证据绑定运行、尝试和产物；旧/伪证据拒绝，取不到证据则待人工核实。 |
| 随使用积累验证 | 记忆与 Skills 质量、语义停滞 | 类型、过期和撤销已有结构规则；仍需证明旧经验有帮助而非污染当前要求。精确重复可检测，换一种措辞反复兜圈尚未可靠识别。 | 固定样本比较启用/停用及冲突场景；语义停滞用独立标注集统计误停与漏停，通过前不自动扩大采用。 |
| 后续扩展 | 价格快照与金额估算 | 已有逐请求 tokens 归因，但不是可靠账单；模型价格与计费口径会变。当前坚持免费路由，金额估算可后排。 | 价格带来源/日期/版本；缺失 tokens 或价格标未知，不凭空算“节省”。 |
| 更强自动执行的前置 | 操作系统级隔离 | 文件副本和目录白名单不能限制命令的全部系统行为。允许更广泛、自动批准的命令前，需要真正的环境隔离。 | 独立验证进程、网络、文件边界；失败不能触及宿主测试范围之外。现阶段继续逐次批准命令，不宣称已有系统沙箱。 |

可延后的扩展：跨任务文件依赖、跨项目团队方案、已有任意复杂流程的任务页重配。它们有使用价值，但不是降低首跑门槛的前提，应在基本闭环和真实样本稳定后按需求推进。代码检查、独立 Electron 测试无需用户打包；验证安装升级、旧数据迁移和最终安装包时才需要本地打包/安装。


## 2026-09-20 第九批 2.17.8：按角色定制与复用团队方案

Git 基线仍为 **74cab88 / 2.17.5**，保留已有未提交的 2.17.6、2.17.7。当前源码 **2.17.8**，本轮未提交、打包、安装、同步 GitHub 或发布。此项开发及独立桌面验收不需要用户先打包。

- **U2 新文本任务配置已实现并验收**：默认简单入口，按需展开分别选择执行、讨论、复核或探索助手的接入/模型/职责；可复制已有 API 成员配置。统一模型修改会真实应用所有角色，个别角色可再调整，实际列表和流程预览同步。
- **团队方案可复用**：项目内保存工作方式与角色模型/职责，支持载入、另存新名及删除；最多 20 份，规范化重名拒绝。保存/载入不执行任务，删除不改当前输入或已有任务。来源成员的权限、技能、自动换路由、思考强度和预算不会隐式继承；新助手使用 medium、30,000 tokens/20 分钟默认值并受项目上限约束。
- **实际执行与历史一致**：通过 UI 分别选择两条本机接入、模型与职责，执行→文本复核→用户批准完成；请求和已保存图均与配置一致。下一任务改模型、另存、删除和准备后，原成员与旧运行快照逐字段不变。完成后重启独立进程也未重发请求。
- **验证**：740/740 全量、18/18 针对性、正式类型、128 文件快速检查及生产构建通过；独立 Electron 实际界面完成保存/刷新/复用/预览/双角色运行/验收/删除/720 px 窄窗口及重启。2 个合成 HTTP 请求、600 夹具 tokens，不代表真实模型质量或服务商费用。证据见 [2.17.8 验收](acceptance/2.17.8/README.md)。
- **边界**：本轮面向尚未准备流程的新文本任务；已有任意流程仍使用原成员/流程编辑器。团队方案限当前项目，文件能力、真实免费模型与新手效果、执行中崩溃恢复继续待做。没有修改日常安装数据或操作 OpenAI 窗口。

**下一工程项：U3 文件任务首跑**，在任务页选择目录与能力，明确读取/修改/命令范围，仍走隔离副本和真实合并确认。随后 U4/U5 运行恢复说明与导航。U0b 与主线真实模型、长期任务、证据、记忆/Skills 和系统沙箱验收继续保留。

## 2026-09-20 第八批 2.17.7：提交检查修复与多任务文本接续

Git 基线仍为 **74cab88 / 2.17.5**，保留上一批未提交的 2.17.6。用户确认本地已更新新版、尚未同步 GitHub；本轮未重新核对安装包版本。当前源码 **2.17.7**，未提交、打包、安装或发布；下方旧版本段落为当时记录。

- **提交阻塞已修复**：check-lite 原先漏算同一行 Record 对象的键，引发 team-project-progress.ts 两处误报。修复扫描器，增加真缺键、嵌套语法和动态键等回归；127 文件快速检查及正式类型检查均通过。
- **U0c-b 文本任务链已实现**：探索建议可包含 1–8 项任务及互斥方向，用户逐项选择、编辑目标/完成标准/先后关系后原子保存。草稿可关闭后继续，也可明确放弃。旧单项格式及旧版已采用记录兼容，避免重复创建。
- **实际执行依赖已接入**：总览显示持久任务关系和等待原因；前置必须完整结束且明确验收。运行保存时固定源运行与实际文本，后续模型请求确实携带；任务后来改名、改目标或重跑不替换旧快照。运行器与主进程分别校验，循环、缺失、自依赖、伪造、未验收、取消及文件产物被拒绝。每项仍单独确认开始，不能自动调度整个项目。
- **验收已通过**：731/731 自动化、正式类型检查、快速检查和生产构建；真实独立 Electron 界面完成草稿编辑/恢复/放弃、保存零请求、前置阻塞、两任务各自开始与批准、实际文本传入、刷新及完成后进程重启。2 个本机合成 HTTP 请求报告 600 tokens，不代表真实供应商费用或模型质量。证据见 [2.17.7 验收](acceptance/2.17.7/README.md)。
- **限制仍明确保留**：当前只支持文本依赖，每任务最多 8 个前置、合计文本最多 32,000 字符，超限拒绝。U0b 真实免费模型与首次使用者、执行中崩溃/并发抢跑桌面组合、文件依赖尚待验证或施工。已保存旧探索流程保留原指令，新准备的探索流程使用新建议格式。

**下一工程优先级**：**U2 按步骤分别选模型、职责定制与已有成员复用**，之后 U3 文件任务首跑、U4/U5 恢复说明与导航。U0b 仍是优先验收项，按既有真实情境协议补样本；本轮没有重试原生控制工具，不把上轮故障当成当前免费入口状态。主线长任务、三策略、客户端证据、记忆/Skills 质量与系统沙箱待办不取消。

## 2026-09-20 第七批 2.17.6：项目阶段总览与来源追溯

工程基线 **74cab88 / 2.17.5**。用户确认已发布、安装，本轮只读核对已安装 app.asar 版本也为 2.17.5。当前源码 **2.17.6**，未提交、打包、安装或发布。以下旧版本段落保留为历史记录。

- **U0c-a 已实现并完成独立 Electron 界面验收**：总览展示探索中、待你决定、待执行、执行中、已完成、需要处理，按紧急程度排序，可筛选；每项任务提供实际原因和准确的下一步入口。待决定直接进入建议或交付确认，中断直接进入核实页。
- **多轮状态保持真实**：不信任旧 task.status；新建空运行不遮住活动运行，另一轮完成不消除未核实/暂停记录；新一轮失败不会被已采用的旧建议覆盖。梳理完成与实际交付完成分开说明，恢复执行后不拿历史错误当当前等待原因。
- **来源与后续任务可追溯**：来源任务、当时建议运行及后续任务均可跳转；来源缺失、不匹配和循环不造成伪造关系或递归。当前只显示来源，尚未实现多任务执行依赖。
- **验证**：702/702 自动化、类型检查、生产构建及差异格式检查通过。独立 Electron 加载最终生产构建，真实点击完成筛选、交付确认入口、中断核实、来源双向跳转、原建议查看、键盘筛选、刷新和 1280/720 px 布局。合成 8 项任务/5 份运行，导航后数量不变。详细限制见 [本轮验收](acceptance/2.17.6/README.md)。
- **U0b 仍待真实验收**：原生桌面控制在重新选择安装版窗口后仍返回“foreground window did not report a process id”。这属于操作工具阻塞，未发起本轮真实模型试验，不能判断免费入口是否可用；合成 UI 测试不能代替真实规划或首次使用者。四类情境与新手记录规范已写入验收目录。

**下一施工优先级**：可操作条件恢复后继续 U0b；独立工程下一项为 **U0c-b 多任务建议与依赖**，再是 U2 角色定制、U3 文件任务首跑、U4/U5 恢复说明与导航。U0c-b 必须覆盖建议逐项编辑/选择、原子保存、循环和缺失依赖拒绝、前置已验收产物绑定，以及运行器和主进程的派发约束。不能先画依赖线就宣称能够自动执行。详细出口见 COLLABORATION_USABILITY.md。

## 2026-09-20 第六批 2.17.5：从模糊想法开始，理解真实协作流程

工程基线 **7c58d7f / 2.17.4**，用户已确认发布、安装；本轮只读核对安装包 app.asar/package.json 也为 2.17.4。以下旧版本段落是历史记录。本轮源码 **2.17.5**，未提交、打包、安装或发布。

用户进一步明确：任务优先仍不够，不能要求初学者先有具体目标和 workflow；一句模糊想法应能开启协作，已有方案应展示实际步骤与分工。

- **U0a 想法起步已实现并经受控桌面通过**：新任务默认“帮我理清想法”，只需一句话；任务名可自动取开头，无需先填验收。也可选择“我知道要做什么”。方向探索、方案整理是两个实际 API 成员步骤，复用原运行器、预算、重试、持久记录与人工结束门。当前仅生成方向、问题及可编辑下一步建议，不自动执行建议。
- **建议到下一任务的接续已通过**：展示格式化结果，支持补充后继续梳理，携带上一轮建议与回答；可把建议目标、完成标准带入编辑器，用户修改保存后才成为独立任务，保留 sourceTaskId/sourceRunId。模型不能通过建议植入成员、工具、路由或流程。无法解析的建议保留原文，下一任务留空填写，失败或未完成步骤不被当作可采用结果。
- **桌面发现的衔接缺陷已修复**：旧轮等待验收占用单并发名额，曾导致下一轮 claim 失败。现在选择下一步会先结束本轮“梳理交付”，不代表同意执行实际方案；独立运行器/持久存储回归复现原失败，再验证名额释放及幂等结束。修复前未派发的运行保留并通过界面停止，未删除或伪造成成功。
- **U1b 真实流程预览已实现**：任务选择、快速方案、流程列表/设计器及运行前检查展示真实图。普通节点用日常表达；可查看成员、指令、产出、条件与下一步，返工保留次数上限。默认选择最新已保存版本，无版本才显示草稿；草稿另有修改时明确提醒。条件/并行/循环保留连接，不强行描绘为直线步骤。任务切换及选择流程后回到页面开头，避免预览被旧滚动位置遮住。
- **规划边界有工程约束**：explore intent 随运行冻结；前端运行器及主进程存储均拒绝带文件/命令工具、技能、本机客户端或执行型节点的探索运行。旧任务无 intent 继续按原语义运行。探索模式不改项目目录、允许接入、权限或现有团队。
- **验证**：684 项自动化、类型检查、生产构建通过；真实 Electron 界面完成想法保存、准备、两轮本机 HTTP 规划、补充、修改建议并保存下一任务、已有复核流程版本预览。4 次模拟请求，合计夹具报告 1200 tokens，无真实供应商费用/质量结论。最新源码哈希、请求、截图和限制见 [本轮验收](acceptance/2.17.5/README.md)。
- **未完成的范围**：真实免费模型的规划质量和首次使用者可理解性未通过本轮证明；尚无自动生成整个多阶段项目及依赖的能力。角色分别选模型/成员复用、文件任务首跑、运行页整体简化与错误恢复引导继续施工。下一阶段优先级已按本次反馈调整。

下一施工顺序：**U0b 真实免费模型与新手验证 → U0c 项目阶段总览及多任务依赖 → U2 角色定制与成员复用 → U3 文件任务首跑 → U4/U5 运行恢复说明与导航收敛**。详细验收条件见 COLLABORATION_USABILITY.md。

## 2026-09-20 第五批 2.17.4：429 退避修正与任务优先入口

用户确认易用性方向为“任务优先，提供可展开的高级设置”。已在保留现有定制能力的前提下完成首批文本入口；后续文件任务和按角色定制继续施工。当前安装版仍为 2.17.2，未提交源码 2.17.4 累积包含上一批 2.17.3；本轮未打包发布。

- **429 问题已定位并修复**：协作空间原本就复用单一 Agent 的 runAgent、全局自动重试设置和默认 62 秒退避；Retry-After 优先，等待受恢复时限、运行预算与取消约束。上轮 OpenRouter 曾自动倒计时，随后由主负责人手动暂停；SenseNova 的 TPM/RPM 原文夹带 insufficient_quota，被共享分类器优先误判为不可重试额度耗尽。现明确分钟限流优先于通用额度代码；402、明确余额耗尽、裸 insufficient_quota 和非分钟日额度仍不自动重试，401/403 维持鉴权优先。
- **真实桌面退避已通过**：本机合成服务返回同类 SenseNova 429 原文，应用显示“62 秒后继续”，实际两次 HTTP 请求相隔 62144 ms，同一路由 429 → 200；没有手动重发或换模型。交付经界面批准 completed，已知夹具用量 200 tokens，首个失败请求用量仍为缺失。这不能证明真实 SenseNova 当时等待后必然可用；旧报告的 quota 分类保留为历史证据。
- **U1 文本快速准备已落地**：任务页选择接入、模型与直接完成/讨论后完成/完成后复核。准备普通成员与可编辑流程，并保存固定版本，下一步确认才派发。空团队实测无需进入成员页或设计器：保存任务 → 准备并检查 → 确认开始 → 验收。准备后 1 成员/1 流程/1 版本，运行和请求数仍为 0。
- **定制与边界**：高级设置保留原有成员、文件流程、已有流程选择及设计器；已有负责人/已有流程不被快速准备覆盖。新快速成员仅处理文本、关闭自动接力、不开放文件/命令，遵守项目允许接入与预算。文件任务首跑、分别选角色模型、团队预设、运行页信息层次和真正的新手对照仍未完成。
- **验证**：655/655 自动化、类型检查、生产构建通过；本机 HTTP 集成验证默认 62000 ms 与 Retry-After:2 的 2000 ms；桌面另以真实时间等待 62.144 秒。i18n 覆盖新增组件。界面检测唯一提示为原有时间线边框，未新增该样式。证据见仓库 docs/acceptance/2.17.4/README.md。
- **支线施工顺序与验收**：U1 文本首跑已完成 → U2 按角色定制与复用 → U3 文件任务首跑 → U4 运行/等待/恢复说明 → U5 导航与新手任务对照。完整条件见 docs/COLLABORATION_USABILITY.md；主线真实模型质量、长任务重启与证据接入继续保留，不因本支线通过而标记完成。

## 2026-09-20 已安装 2.17.2，第四批 2.17.3 工程及受控桌面验收完成

用户确认 2.17.2 已打包发布并安装；本机安装包 app.asar 版本核实为 2.17.2，仓库基线为 ecc5578。日常项目、对话和历史协作记录在升级后仍可见，未据此宣称全部旧数据语义迁移已验收。以下旧版本段落记录的是当时状态，不能再理解为当前尚未安装。

- **A5 精确重复返工停止已完成**：相同复核节点连续失败、内容和要求不变且没有新增可区分证据时，保存失败并停止派发，等待核实。按文件哈希、文本摘要、读取/引用内容比较，忽略新编号和时间戳；新增用户补充要求也会阻止误判。内容/要求/证据变化或无法可靠规范化时仍沿用原有限回路，不判断语义等价。
- **桌面发现并修复重试缺陷**：真实 runAgent 在明确重试时曾拼接旧、新 JSON。现持久保存停滞标记，仅该类已完成复核使用新检查点重新生成；原回复、检查证据和用量保留，普通中断恢复逻辑不变。回归先复现失败，再验证修复。
- **实际桌面闭环已通过**：独立 Electron 窗口操作新建运行，两轮相同文本与失败引用后暂停；重载窗口后明确重试，仅新增一轮复核，再次停滞；人工接受后仍进入最终交付确认，经按钮批准完成。2 次执行、3 次复核、5 个本机 HTTP 请求，夹具实报 1000 tokens，旧请求未重复计数，无第三轮执行。失败判词仍保留 fail；这证明控制流程，不证明模型质量或 OS 进程重启。
- **工程检查已通过**：646/646 自动化、类型检查、生产构建、差异格式检查。只读证据校验确认 5 个不同请求 ID、冻结输入一致、旧检查点保留、预算预留清空；构建保留已有大包提示。详见仓库 docs/acceptance/2.17.3/README.md。
- **安装版免费路由试验有记录，质量对照未完成**：在已安装 2.17.2 界面新建合成 CSV 项目、导入三策略模板并保存单成员版本，分别新建三次单成员运行。SenseNova deepseek-v4-flash 为 HTTP 429 / insufficient_quota；omni auto/coding:free 经网关选择 oc/big-pickle 后返回 HTTP 403 客户端限制；OpenRouter qwen/qwen3.8-27b:free 三次 HTTP 429 后经界面暂停。共 5 个请求，均无产物，输入未改动；用量缺失，不能记作零。仅选择授权免费入口，未直接选择 dva/ 或 oc/，自动接力关闭。这些固定模型当次失败，不代表整个平台不可用。
- **交付边界**：当前源码 2.17.3 尚未提交、打包、安装或发布。日常应用及 OpenAI 窗口保持打开。真实三策略对照、长任务实际进程重启、订阅客户端证据桥、记忆/Skills 质量评估与系统沙箱仍未完成；下一步顺序和验收出口见路线图。

## 2026-09-20 测试路由更正与交付安排

用户已明确测试范围：SenseNova 全部模型、omni 的 auto/ 路由、OpenRouter 的免费路由。此前 dva/gpt-5-6-luna-max 和 oc/gpt-5.6-luna 选型不符合此范围；两次 500/402 只证明这些路由当时失败，不能据此认定免费路由或整个网关不可用。免费路由的真实任务验收仍待进行，后续不得沿用前述两条付费路由。历史失败记录保留，不将缺失用量解释为零费用。

当前 2.17.2 可冻结代码并制作待验收安装包；对外正式发布前补安装启动、旧数据保留、免费路由真实任务和重启接续的基本检查。更大规模的三策略收益比较可在后续版本继续，不能在本次说明中宣称已证明质量或节省成本。本记录没有执行打包、安装、上传或发布。

当前工程基线：2.17.2 / ecc5578；当前未提交源码：2.17.4。2.16.2 / cf70b1b 是最初审查基线，首三批状态保留于历史段落。用户已授权按顺序实现、实际操作应用测试并持续更新进度。条件来源：工作区 `wickrun-collaboration-priorities-2.16.2.md`；仓库快照见 [NEXT_STAGE_ACCEPTANCE.md](NEXT_STAGE_ACCEPTANCE.md)。执行事实以 [PROGRESS.md](../PROGRESS.md) 为准。

## 施工顺序与交付门槛

| 批次 | 工作项 | 对应条件 | 验收与依赖 |
|---|---|---|---|
| S0 | 建立路线图、进度、固定基线和隔离工作目录 | 全部 | 所有条目有状态、证据与未覆盖范围 |
| S1 | 预算硬上限一致性、本机成员完成判定 | P0/A9 | 小成员预算不得被抬高；合法配置可以执行工具；修改/测试/推送分开 |
| S2 | 最小持久任务契约、有效要求、压缩回归 | A1/A2/A3 | 纠正、否定、失败/未知状态、来源和多次压缩不会被结构性丢弃；明确语义判断边界 |
| S3 | 版本化产物快照、跨成员接收、依赖输入选择 | A1/A4/A7 | 不动主目录；新增/修改/删除可交接；版本冲突拒绝；重试幂等 |
| S4 | review 证据、版本校验、只读复核与有限返工 | A5 | 伪证据/旧版本不能通过；生成→找错→修复→复核可运行 |
| S5 | 故障传输链路、恢复和定时去重 | A8 | 本地 HTTP 429 后检查点接力；未知副作用不重发；重启/调度不重复 |
| S6 | 产品界面和原生桌面闭环验收 | A6/A4 | 实际操作测试实例，生成产物、复核与返工、人工验收、差异合并；截图/日志入进度 |
| S7 | 成本归因、记忆检索/失效、调度对照 | A2/A9/A10 | 复用既有记录；未知用量保留；撤销经验不注入；同任务对照不以自评替代结果 |
| S8 | 全量验证、版本同步、条件与路线图回写 | 全部 | 测试、类型检查、构建；明确本地模拟与真实模型实测的界限 |

## 实施原则

- 主执行器、存储、工具与 UI 的现有路径优先，不引入第二套运行引擎或数据库。
- Agent 在独立目录工作；主负责人核对差异并整合。子任务完成声明不等于验收。
- 程序测试、受控传输测试、真实桌面操作、真实供应商试验分开记录。
- 不自动发布、安装替换日常应用或使用生产项目文件做故障注入。
- 系统沙箱扩展、供应商原生压缩、大规模自动路由、自动经验采用属于条件中的后续决策项：先记录能力边界和对照证据，不凭一轮成功启用。

## 状态

首批 S0–S6 的实现与受控桌面验收已完成，S7 的用量归因、记忆失效和调度边界已补齐；S8 已完成最终 609 项测试、类型检查、构建与记录。真实供应商质量评估及策略收益对照尚未完成，不归入本批通过结论。

施工中新增的实际修正：产物接收恢复日志、接收副本篡改检查、复核副本隐藏合并动作、本机复核能力的派发前检查、任务契约注入长度上限、暂停/未知状态阻止定时重叠。

## 第二批 2.17.1 的实际推进

| 项目 | 实际状态 | 证据与出口 |
|---|---|---|
| 对照前提：逐请求路由 | 已完成 | 派发身份记录、去重、脱敏请求事件；多路由任务总量仍不重复归因 |
| 组合生命周期 | 受控回归已通过 | 纠正→多次压缩→持久化重载→换连接/模型→证据检索；未知动作需明确处理 |
| 复核证据漏洞 | 已修复 | 完整接收路径匹配；同名异路径及无版本产物不能通过 |
| 真实模型对照 | 已尝试，外部阻塞 | 桌面 Luna Max 返回 500 沙箱配置错误；另一固定 Luna 路由返回 402；三策略尚无质量结论 |
| 总验收 | 工程与受控桌面完成 | 617/617、类型与构建通过；2.17.1 桌面新运行完成 fail→返工→pass→批准，详见 docs/acceptance/2.17.1 |

先做用量与组合回归，是为真实策略试验准备可核验的测量基础；未改变最初的质量优先目标。此批未打包发布，日常应用和 OpenAI 窗口保留。

## 第三批 2.17.2 的实际推进

此前误选两条付费路由失败，本批先完成不依赖外部可用性的 A5/A10/A9 扩展：纯文本版本与原文复核、冻结快照的记忆类型/范围/过期/有界选择、完整逐请求路由聚合与导出。实际桌面跑通 fail→返工→pass→批准，同时验证两条路由各 360 tokens、记忆仅选入 2 条。638 项自动化及构建通过。真实模型质量与策略收益仍未验收。

桌面发现生成流程时长可能超过项目上限，已让新模板预算收敛到项目设置。复查补上混合旧请求的成本缺口以及旧讨论记录的成员归属。此批没有自动购买额度、修改网关或关闭现有应用窗口。

## 第四批与下一步验收出口（2026-09-20 更新）

| 顺序 | 工作与当前状态 | 完成条件 |
|---|---|---|
| 本批完成 | A5 精确停滞与一次明确重试 | 文件/文本自动化；实际文本桌面两轮停止、重载后仅新增一次复核、再次停止、人工接受后仍需交付批准；已通过，见 [2.17.3 证据](acceptance/2.17.3/README.md) |
| 1 | 免费固定模型与三策略试验：已尝试，尚无有效产物 | 先用另一授权免费固定模型完成 CSV 题并校验输入不变和输出答案，再固定同题/模型/预算分别执行单成员、讨论后执行、执行后复核；记录验收、错误完成、tokens、耗时和未核实比例。缺失用量保持未知；不能从三次入口失败推断平台不可用 |
| 2 | 长任务组合：受控自动化已通过，真实语义与进程重启未验收 | 同一任务内包含纠正、至少两次压缩、跨供应商切换、真正进程重启和原始证据检索；已成功操作不重做，未知副作用需核实，要求逐条对照。用独立实例进行，避免关闭日常应用 |
| 3，可在供应商受阻时独立施工 | 订阅客户端证据桥：未实现 | 明确客户端能提供的真实工具/文件/测试证据，绑定运行、尝试与产物；拒绝伪造/旧版本/无证据完成，真实桌面验证。不可获得证据时仍待人工核实 |
| 4 | 记忆与 Skills 质量：结构治理已实现，对照未做 | 固定任务集比较启用/停用；最新要求冲突、过期、项目隔离和撤销均留证据。采用需可追溯，不凭模型自评分自动采用 |
| 5 | 语义停滞与价格：未实现 | 精确重复现已覆盖，语义相似仍需独立标注集验证误停/漏停；价格需版本化来源、日期和缺失规则，未知用量不得推算费用 |
| 6 | 系统执行环境：未实现 | 在独立工程中验证进程、网络及文件的系统级隔离；工作目录和文件副本不能作为完整沙箱证明 |

交付规则保持不变：每项分别记录源码、自动化、实际桌面和真实供应商状态。当前源码为 2.17.4，安装版仍为 2.17.2；尚未执行打包发布。易用性支线 U1 已完成，U2–U5 的施工与验收见 [协作空间易用性](COLLABORATION_USABILITY.md)。
