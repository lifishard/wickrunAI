# wickrunAI 工程进度

## 2026-09-20 第六批 2.17.5：从模糊想法开始，理解真实协作流程

工程基线 **7c58d7f / 2.17.4**，用户已确认发布、安装；本轮只读核对安装包 app.asar/package.json 也为 2.17.4。以下旧版本段落是历史记录。本轮源码 **2.17.5**，未提交、打包、安装或发布。

用户进一步明确：任务优先仍不够，不能要求初学者先有具体目标和 workflow；一句模糊想法应能开启协作，已有方案应展示实际步骤与分工。

- **U0a 想法起步已实现并经受控桌面通过**：新任务默认“帮我理清想法”，只需一句话；任务名可自动取开头，无需先填验收。也可选择“我知道要做什么”。方向探索、方案整理是两个实际 API 成员步骤，复用原运行器、预算、重试、持久记录与人工结束门。当前仅生成方向、问题及可编辑下一步建议，不自动执行建议。
- **建议到下一任务的接续已通过**：展示格式化结果，支持补充后继续梳理，携带上一轮建议与回答；可把建议目标、完成标准带入编辑器，用户修改保存后才成为独立任务，保留 sourceTaskId/sourceRunId。模型不能通过建议植入成员、工具、路由或流程。无法解析的建议保留原文，下一任务留空填写，失败或未完成步骤不被当作可采用结果。
- **桌面发现的衔接缺陷已修复**：旧轮等待验收占用单并发名额，曾导致下一轮 claim 失败。现在选择下一步会先结束本轮“梳理交付”，不代表同意执行实际方案；独立运行器/持久存储回归复现原失败，再验证名额释放及幂等结束。修复前未派发的运行保留并通过界面停止，未删除或伪造成成功。
- **U1b 真实流程预览已实现**：任务选择、快速方案、流程列表/设计器及运行前检查展示真实图。普通节点用日常表达；可查看成员、指令、产出、条件与下一步，返工保留次数上限。默认选择最新已保存版本，无版本才显示草稿；草稿另有修改时明确提醒。条件/并行/循环保留连接，不强行描绘为直线步骤。任务切换及选择流程后回到页面开头，避免预览被旧滚动位置遮住。
- **规划边界有工程约束**：explore intent 随运行冻结；前端运行器及主进程存储均拒绝带文件/命令工具、技能、本机客户端或执行型节点的探索运行。旧任务无 intent 继续按原语义运行。探索模式不改项目目录、允许接入、权限或现有团队。
- **验证**：684 项自动化、类型检查、生产构建通过；真实 Electron 界面完成想法保存、准备、两轮本机 HTTP 规划、补充、修改建议并保存下一任务、已有复核流程版本预览。4 次模拟请求，合计夹具报告 1200 tokens，无真实供应商费用/质量结论。最新源码哈希、请求、截图和限制见 [本轮验收](docs/acceptance/2.17.5/README.md)。
- **未完成的范围**：真实免费模型的规划质量和首次使用者可理解性未通过本轮证明；尚无自动生成整个多阶段项目及依赖的能力。角色分别选模型/成员复用、文件任务首跑、运行页整体简化与错误恢复引导继续施工。下一阶段优先级已按本次反馈调整。

下一施工顺序：**U0b 真实免费模型与新手验证 → U0c 项目阶段总览及多任务依赖 → U2 角色定制与成员复用 → U3 文件任务首跑 → U4/U5 运行恢复说明与导航收敛**。详细验收条件见 COLLABORATION_USABILITY.md。

## 2026-09-20 第五批 2.17.4：429 退避修正与任务优先入口

用户确认易用性方向为“任务优先，提供可展开的高级设置”。已在保留现有定制能力的前提下完成首批文本入口；后续文件任务和按角色定制继续施工。当前安装版仍为 2.17.2，未提交源码 2.17.4 累积包含上一批 2.17.3；本轮未打包发布。

- **429 问题已定位并修复**：协作空间原本就复用单一 Agent 的 runAgent、全局自动重试设置和默认 62 秒退避；Retry-After 优先，等待受恢复时限、运行预算与取消约束。上轮 OpenRouter 曾自动倒计时，随后由主负责人手动暂停；SenseNova 的 TPM/RPM 原文夹带 insufficient_quota，被共享分类器优先误判为不可重试额度耗尽。现明确分钟限流优先于通用额度代码；402、明确余额耗尽、裸 insufficient_quota 和非分钟日额度仍不自动重试，401/403 维持鉴权优先。
- **真实桌面退避已通过**：本机合成服务返回同类 SenseNova 429 原文，应用显示“62 秒后继续”，实际两次 HTTP 请求相隔 62144 ms，同一路由 429 → 200；没有手动重发或换模型。交付经界面批准 completed，已知夹具用量 200 tokens，首个失败请求用量仍为缺失。这不能证明真实 SenseNova 当时等待后必然可用；旧报告的 quota 分类保留为历史证据。
- **U1 文本快速准备已落地**：任务页选择接入、模型与直接完成/讨论后完成/完成后复核。准备普通成员与可编辑流程，并保存固定版本，下一步确认才派发。空团队实测无需进入成员页或设计器：保存任务 → 准备并检查 → 确认开始 → 验收。准备后 1 成员/1 流程/1 版本，运行和请求数仍为 0。
- **定制与边界**：高级设置保留原有成员、文件流程、已有流程选择及设计器；已有负责人/已有流程不被快速准备覆盖。新快速成员仅处理文本、关闭自动接力、不开放文件/命令，遵守项目允许接入与预算。文件任务首跑、分别选角色模型、团队预设、运行页信息层次和真正的新手对照仍未完成。
- **验证**：655/655 自动化、类型检查、生产构建通过；本机 HTTP 集成验证默认 62000 ms 与 Retry-After:2 的 2000 ms；桌面另以真实时间等待 62.144 秒。i18n 覆盖新增组件。界面检测唯一提示为原有时间线边框，未新增该样式。证据见仓库 docs/acceptance/2.17.4/README.md。
- **支线施工顺序与验收**：U1 文本首跑已完成 → U2 按角色定制与复用 → U3 文件任务首跑 → U4 运行/等待/恢复说明 → U5 导航与新手任务对照。完整条件见 docs/COLLABORATION_USABILITY.md；主线真实模型质量、长任务重启与证据接入继续保留，不因本支线通过而标记完成。

## 2026-09-20 已安装 2.17.2，第四批 2.17.3 工程及受控桌面验收完成

用户确认 2.17.2 已打包发布并安装；本机安装包 app.asar 版本核实为 2.17.2，仓库基线为 ecc5578。日常项目、对话和历史协作记录在升级后仍可见，未据此宣称全部旧数据语义迁移已验收。以下旧版本段落记录的是当时状态，不能再理解为当前尚未安装。

- **A5 精确重复返工停止已完成**：相同复核节点连续失败、内容和要求不变且没有新增可区分证据时，保存失败并停止派发，等待核实。按文件哈希、文本摘要、读取/引用内容比较，忽略新编号和时间戳；新增用户补充要求也会阻止误判。内容/要求/证据变化或无法可靠规范化时仍沿用原有限回路，不判断语义等价。
- **桌面发现并修复重试缺陷**：真实 runAgent 在明确重试时曾拼接旧、新 JSON。现持久保存停滞标记，仅该类已完成复核使用新检查点重新生成；原回复、检查证据和用量保留，普通中断恢复逻辑不变。回归先复现失败，再验证修复。
- **实际桌面闭环已通过**：独立 Electron 窗口操作新建运行，两轮相同文本与失败引用后暂停；重载窗口后明确重试，仅新增一轮复核，再次停滞；人工接受后仍进入最终交付确认，经按钮批准完成。2 次执行、3 次复核、5 个本机 HTTP 请求，夹具实报 1000 tokens，旧请求未重复计数，无第三轮执行。失败判词仍保留 fail；这证明控制流程，不证明模型质量或 OS 进程重启。
- **工程检查已通过**：646/646 自动化、类型检查、生产构建、差异格式检查。只读证据校验确认 5 个不同请求 ID、冻结输入一致、旧检查点保留、预算预留清空；构建保留已有大包提示。详见仓库 docs/acceptance/2.17.3/README.md。
- **安装版免费路由试验有记录，质量对照未完成**：在已安装 2.17.2 界面新建合成 CSV 项目、导入三策略模板并保存单成员版本，分别新建三次单成员运行。SenseNova deepseek-v4-flash 为 HTTP 429 / insufficient_quota；omni auto/coding:free 经网关选择 oc/big-pickle 后返回 HTTP 403 客户端限制；OpenRouter qwen/qwen3.8-27b:free 三次 HTTP 429 后经界面暂停。共 5 个请求，均无产物，输入未改动；用量缺失，不能记作零。仅选择授权免费入口，未直接选择 dva/ 或 oc/，自动接力关闭。这些固定模型当次失败，不代表整个平台不可用。
- **交付边界**：当前源码 2.17.3 尚未提交、打包、安装或发布。日常应用及 OpenAI 窗口保持打开。真实三策略对照、长任务实际进程重启、订阅客户端证据桥、记忆/Skills 质量评估与系统沙箱仍未完成；下一步顺序和验收出口见路线图。

## 2026-09-20 测试路由更正与交付安排

用户已明确测试范围：SenseNova 全部模型、omni 的 auto/ 路由、OpenRouter 的免费路由。此前 dva/gpt-5-6-luna-max 和 oc/gpt-5.6-luna 选型不符合此范围；两次 500/402 只证明这些路由当时失败，不能据此认定免费路由或整个网关不可用。免费路由的真实任务验收仍待进行，后续不得沿用前述两条付费路由。历史失败记录保留，不将缺失用量解释为零费用。

当前 2.17.2 可冻结代码并制作待验收安装包；对外正式发布前补安装启动、旧数据保留、免费路由真实任务和重启接续的基本检查。更大规模的三策略收益比较可在后续版本继续，不能在本次说明中宣称已证明质量或节省成本。本记录没有执行打包、安装、上传或发布。

## 第三批 2.17.2（工程及受控桌面验收完成）

当前源码 **2.17.2**，包含前两批修改；未提交、未打包、未发布或替换日常安装。

| 工作 | 实际状态 | 验收与边界 |
|---|---|---|
| 纯文本独立复核 | 实现、自动化和受控桌面通过 | 当前 attempt/版本/SHA-256/原文引用绑定，20,000 字符整段上限；拒绝旧版本、虚构引用、漏引输入和文件改动冒充文本；模型判断不等于事实证明 |
| 项目记忆筛选 | 实现、自动化和受控请求通过 | 事实/偏好/经验、项目/任务关键词、过期时间；冻结全部已采用版本，按运行创建时点派生最多 6000 字符；整条略过并显示原因，候选不自动采用 |
| 逐路由用量汇总 | 实现、自动化通过 | 从完整检查点去重汇总，独立于 200 条事件裁剪；缺失身份及旧记录缺口保持未知；导出保留脱敏汇总；不推算金额或模型因果贡献 |
| 桌面发现的生成预算问题 | 已修复并经最终界面复查 | 新生成流程的时长和 tokens 收敛至项目上限；此前超限流程被运行校验拦截，未发请求；重载最终构建后再生成，自动采用 10 分钟项目上限 |
| 复查发现的统计问题 | 已修复 | 新旧请求混合时不能宣称路由成本完整；旧讨论检查点依派发身份选择成员，不能默认归到首位成员 |
| 全量验证 | 已通过 | 638/638、类型检查（构建内）、生产构建、差异格式检查；既有大包警告保留 |

已在独立 wickrunAI 窗口从产品按钮生成流程、保存版本、启动运行，完成 fail → 返工 → pass → 界面批准。4 个本机受控 HTTP 请求共报告 720 tokens，两条路由各 360；这是夹具用量，不是真实供应商成本。请求检查确认只注入 2 条、173 字符记忆；不匹配、已过期、超长和候选标记未出现在请求中。没有文件隔离区或文件操作；文本模式未下发文件/外部工具，通用澄清工具 request_user_input 仍由基础引擎提供。

界面重载后任务仍为 completed；通过界面修订记忆关键词后保存为候选 v2，旧运行仍保留已采用 v1。最后重新生成流程确认预算修正生效。

Sol 实现路由汇总和文本复核回归；Luna 实现记忆选择并独立复查用量边界；主负责人审查整合、修正边界、接入界面并亲自完成桌面操作。证据目录：[2.17.2](docs/acceptance/2.17.2/README.md)。

## 第二批 2.17.1 历史记录（真实模型对照受阻）

当前源代码版本 **2.17.1**，包含首批修改；未提交、未打包、未发布或替换日常安装。

| 工作 | 当前状态 | 证据与限制 |
|---|---|---|
| 逐请求路由归属 | 已完成并验收 | 派发连接/模型、脱敏路由事件、稳定 ID 去重；相关 12 项测试；多路由任务级排行榜与价格仍待做 |
| 长任务组合 | 受控回归通过 | 实际 runAgent 串联纠正、至少两次压缩、持久化重载、换连接/模型、原证据检索、成功写入不重做、未知动作明确跳过；归档位置构造，未证明真实摘要语义或实际进程重启 |
| 复核路径漏洞 | 已修复 | 完整接收路径核对；同名异路径、POSIX 大小写及无版本产物的通过被拒；8 项针对性测试 |
| 固定策略试验题 | 已建立 | 相同 CSV、三种流程、独立 JSON 与输入字节校验；离线报告工具可复跑 |
| 真实模型桌面 | 失败已记录，外部阻塞 | 在应用 UI 启动固定 Luna Max，网关 500：DEVIN_AGENTIC_HOME 沙箱配置无效；固定替代 Luna 连通性请求 402 payment_required；没有可用产物，不报告质量收益 |
| 2.17.1 受控桌面 | 已通过 | 新建运行 fail→返工→pass→界面批准；v1/v2 均有实际差异，两个隔离文件为 ready；8 请求均带连接/模型身份 |
| 全量检查 | 已通过 | **617/617**、类型检查、生产构建和差异格式检查；既有大包警告保留 |

验收证据：[2.17.1 报告](docs/acceptance/2.17.1/README.md)、[真实模型试跑数据](docs/acceptance/2.17.1/strategy-pilot.json)。职责：Sol 实现路由归属，Luna 增加组合回归；主负责人审查整合、增加跨连接断言、修复路径漏洞并操作桌面验收。

真实模型的输入/输出用量没有返回，**不能按 0 计费**。界面显示的 3,659 是运行预算保守记账，不是供应商实报 tokens。没有改动网关沙箱或购买额度。

## 首批 2.17.0 历史记录

工程基线：2.16.2 / cf70b1b。首批已实现版本：**2.17.0**，当前为未提交工作树；未发布、未替换日常安装。原始《条件》与 [仓库副本](docs/NEXT_STAGE_ACCEPTANCE.md) 已同步；[施工路线图](docs/ENGINEERING_ROADMAP.md) 已按实际状态更新。

状态规则：代码实现、自动化通过、桌面通过、真实供应商通过分别记录。下表“已验收”仅指明确列出的本批范围。

| ID | 工作 | 当前状态 | 验收证据与边界 |
|---|---|---|---|
| S0 | 基线、路线图与分工 | 已完成 | Sol/Luna 各自在隔离快照修改，主负责人核对并整合 |
| S1 | 预算/本机完成判定 | 本批已验收 | 真实权限检查验证成员上限；修改有真实 diff 可结束，无修改或要求测试/推送仍保留缺口 |
| S2 | 任务契约/压缩回归 | 本批已验收（自动化） | 纠正、否定、未知副作用、来源、反复压缩/换模型；20 万字符原文下投影有界且原文可检索；语义保真未证明 |
| S3 | 产物快照与交接 | 本批已验收 | 新增/修改/删除、版本替换/归零、冲突、跨范围拒绝、篡改与中断恢复；真实桌面完成两人交接 |
| S4 | 独立复核与返工 | 本批已验收 | 真实引擎+文件后端+权限的 fail→rework→pass；伪编号/旧版本/无关文件/接收后改动拒绝；实际桌面复现 |
| S5 | HTTP 故障/恢复/调度 | 本批已验收（自动化） | 实际 localhost HTTP 429、检查点接力、不重做已成功写入、未知副作用停止；并发触发去重、暂停/未知旧任务防重叠 |
| S6 | 真实桌面闭环 | 本批已验收（受控模型） | 原生界面创建项目、开始运行、差异查看、批准、合并；最终 JSON 为 ready；重启后保存记录保留，新模板由界面生成保存并再运行 |
| S7 | 成本/记忆/策略 | 基础已验收；策略收益待评估 | 稳定请求 ID 去重、普通/最终/压缩用途记录进入团队统计；记忆停用/项目隔离；真实供应商策略对照与多路由精确归属未完成 |
| S8 | 总验收与文档回写 | 已完成 | 全量 **609/609**，类型检查和生产构建通过，差异格式检查通过；版本/锁文件/界面常量/README 同步为 2.17.0 |

## 验证账本

- 最终全量：609 通过、0 失败、0 跳过。日志：`docs/acceptance/2.17.0/automated-tests.txt`。
- 最终构建包含 TypeScript 检查与 Vite 构建，均通过；保留现有大包体积警告。日志：`docs/acceptance/2.17.0/build.txt`。
- 桌面步骤、截图、产物版本、读取调用编号、交付批准事件和最终文件哈希：[桌面验收记录](docs/acceptance/2.17.0/README.md)。
- 第一轮经过实际 Electron IPC、主进程网络/工具、隔离文件及合并，模型由受控本地 HTTP 服务驱动；不能标成真实 Claude/OpenAI 模型质量通过。
- 第二轮从应用按钮生成并保存新流程，再跑 fail→返工→pass。原主目录已有 ready，因此最终 v2 是“回到基线”的空差异快照；两位成员隔离区均读回 ready，覆盖版本归零的桌面路径。
- 历史 112/579 等数字属于基线报告；本批验收不沿用它们。

## 施工发现与实际修正

1. 阶段建议下限可能突破成员预算，改为受成员硬上限约束。
2. 本机仅修改任务被无条件判为缺测试证据，拆开判断。
3. 跨成员原本只传文字；现发布不可变文件版本并在接收端保留来源。
4. 不仅核对发布者，也核对复核副本；接收中断有日志与恢复。
5. 复核副本曾显示重复合并入口；最终界面已隐藏并说明用途。
6. 新流程生成后需要绑定原任务；现在生成时同步绑定。
7. 任务契约首版注入完整历史会造成重复开销；整合前改成有限投影与检索引用。
8. 团队统计丢弃已有请求用量；新增稳定 ID，去重后投影。缺失不当作零。
9. 定时跳过规则遗漏 paused/uncertain，已补齐。
10. 历史试点模板使用本机复核席位；新证据要求下明确在派发前提示改用 API，模板兼容测试已更新。

## 下一批与未通过项

- 固定真实任务集，对照单成员、讨论后执行、执行后复核的质量/错误完成/成本/耗时；尚无供应商收益结论。
- 同一长任务同时经历用户纠正、压缩、模型切换、重启的真实质量试验。
- 原生订阅客户端证据桥、语义停滞检测和真实文本复核质量对照；文本版本与引用检查已完成。
- 记忆语义相关性/最新要求冲突评估、Skills 受控评估和定价快照；类型/范围/过期/字面筛选及多路由 token 聚合已完成。
- 系统级执行沙箱；当前文件副本不隔离任意 shell 副作用。

OpenAI 窗口与日常 wickrunAI 窗口均保留。曾关闭并重启的仅为工程验收隔离实例；用户提出不要关闭 OpenAI 后，核对窗口仍在，后续未再执行关闭窗口操作。
